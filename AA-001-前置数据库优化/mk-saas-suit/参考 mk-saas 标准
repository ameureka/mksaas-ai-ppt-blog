

  规范（仅有一份 Drizzle 规范提示）

  - .cursor/rules/drizzle-orm-best-practices.mdc：建议使用 Drizzle 类型安全查询、通过 Drizzle Kit 迁移、使用 relation 定义关联、事务处理复杂原子操作。

  表与结构（来源：src/db/schema.ts 与 src/db/migrations/0000-0001)

  - user：账号基础信息（角色、封禁字段、customer_id）；主键 id，邮箱唯一；索引 user_id_idx/user_customer_id_idx/user_role_idx。
  - session：登录会话；token 唯一；外键 user_id 级联删除；索引 session_token_idx/session_user_id_idx。
  - account：社交/本地账号绑定；外键 user_id 级联删除；索引 account_user_id_idx/account_account_id_idx/account_provider_id_idx。
  - verification：验证码/验证令牌，无外键。
  - payment：支付记录（price/type/scene/interval/subscription/session/invoice/paid/status等）；外键 user_id 级联删除；多索引覆盖类型/场景/price/user/customer/status/paid/subscription/session/invoice。
  - user_credit：积分余额（current_credits、last_refresh_at 标记废弃）；外键 user_id 级联删除；索引 user_credit_user_id_idx。
  - credit_transaction：积分流水（type/amount/remaining_amount/payment_id 注释为 invoiceId/过期时间）；外键 user_id 级联删除；索引 credit_transaction_user_id_idx/credit_transaction_type_idx。

  未见 PPT/广告/下载等业务表；迁移目录仅含 0000_fine_sir_ram.sql（auth/payment）和 0001_woozy_jigsaw.sql（credit/transaction）

  - Schema 定义：src/db/schema.ts（Drizzle ORM 全部表/索引定义）。
  - 迁移脚本：
      - Drizzle 自动迁移：src/db/migrations/*.sql（0000 开始）。
      - 手工 SQL：migrations/create_ad_tables.sql（广告/下载表）。
  - 配置入口：drizzle.config.ts（迁移目录/连接配置）。
  - 设计与优化文档：AA-001-前置数据库优化/ 目录下的方案、specs、status-quo 等 Markdown。
  - 运行时连接：.env.local 中的 DATABASE_URL 等。


