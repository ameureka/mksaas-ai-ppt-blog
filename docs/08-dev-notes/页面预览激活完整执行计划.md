# 页面预览激活完整执行计划
**创建日期**: 2025-11-26
**目标**: 实现 100% 页面可预览，符合 mk-saas 设计标准
**原则**: Mock 数据优先，不涉及数据库，遵循设计规范

---

## 🎯 **执行目标**

### 当前状态
```
总页面数: 39

✅ 完全可预览:    33/39  (84.6%)
⚠️ 需要改进:       4/39  (10.3%)
❌ 需要修复:       2/39  (5.1%)
```

### 目标状态
```
总页面数: 39

✅ 完全可预览:    39/39  (100%)
✅ 符合规范:      39/39  (100%)
✅ Mock 交互:     100%   (所有交互功能正常)
```

---

## 📋 **任务清单总览**

### 阶段 1: 高优先级修复 (必须完成) - 4 小时
- [ ] Task 1.1: 修复 `/admin/ppts-v0` 页面 (2 小时)
- [ ] Task 1.2: 修复 `/admin/users-v0` 页面 (2 小时)

### 阶段 2: 中优先级改进 (建议完成) - 1.5 小时
- [ ] Task 2.1: 添加表单 Actions (30 分钟)
- [ ] Task 2.2: 检查 AI 页面 (30 分钟)
- [ ] Task 2.3: 验证所有 Admin 页面规范 (30 分钟)

### 阶段 3: 全面测试验证 (必须完成) - 1 小时
- [ ] Task 3.1: 页面可访问性测试 (20 分钟)
- [ ] Task 3.2: Mock 数据和交互测试 (20 分钟)
- [ ] Task 3.3: 规范符合度验证 (20 分钟)

**总预计时间**: 6.5 小时 (1 个工作日)

---

## 📝 **详细任务执行计划**

---

## 🔴 **阶段 1: 高优先级修复**

### Task 1.1: 修复 `/admin/ppts-v0` 页面

**当前状态**: ❌ 违反 5 项规范
- 未使用 AdminPageShell
- 没有 Mock 数据
- 没有 Client Component
- 交互逻辑缺失
- 返回空数组

**目标**: ✅ 100% 符合 mk-saas 设计标准

---

#### 子任务 1.1.1: 创建 PPT Mock 数据 (30 分钟)

**文件**: `src/lib/mock-data/ppts.ts`

```typescript
// 新建文件: src/lib/mock-data/ppts.ts

export interface MockPPT {
  id: string
  title: string
  category: 'business' | 'education' | 'marketing' | 'summary' | 'proposal' | 'training' | 'report' | 'plan'
  status: 'draft' | 'published' | 'archived'
  downloads: number
  views: number
  updatedAt: string
  author?: string
  description?: string
}

export function generateMockPPTs(count: number): MockPPT[] {
  const categories = ['business', 'education', 'marketing', 'summary', 'proposal', 'training', 'report', 'plan'] as const
  const statuses = ['draft', 'published', 'archived'] as const

  return Array.from({ length: count }, (_, i) => ({
    id: `ppt_${String(i + 1).padStart(3, '0')}`,
    title: `PPT 模板 ${i + 1} - ${getCategoryName(categories[i % categories.length])}`,
    category: categories[i % categories.length],
    status: statuses[i % statuses.length],
    downloads: Math.floor(Math.random() * 5000) + 100,
    views: Math.floor(Math.random() * 10000) + 500,
    updatedAt: new Date(Date.now() - Math.random() * 90 * 24 * 60 * 60 * 1000).toISOString(),
    author: `作者 ${Math.floor(i / 5) + 1}`,
    description: `这是 PPT 模板 ${i + 1} 的详细描述，包含丰富的内容和专业的设计。`,
  }))
}

function getCategoryName(category: string): string {
  const names: Record<string, string> = {
    business: '商务报告',
    education: '教育培训',
    marketing: '营销策划',
    summary: '工作总结',
    proposal: '项目提案',
    training: '培训课件',
    report: '述职报告',
    plan: '营销计划',
  }
  return names[category] || category
}

// 导出预生成的 Mock 数据
export const MOCK_PPTS = generateMockPPTs(50)

// 按分类分组
export const MOCK_PPTS_BY_CATEGORY = MOCK_PPTS.reduce((acc, ppt) => {
  if (!acc[ppt.category]) acc[ppt.category] = []
  acc[ppt.category].push(ppt)
  return acc
}, {} as Record<string, MockPPT[]>)

// 按状态分组
export const MOCK_PPTS_BY_STATUS = MOCK_PPTS.reduce((acc, ppt) => {
  if (!acc[ppt.status]) acc[ppt.status] = []
  acc[ppt.status].push(ppt)
  return acc
}, {} as Record<string, MockPPT[]>)
```

**验收标准**:
- ✅ 文件创建成功
- ✅ TypeScript 类型无错误
- ✅ 生成 50 个 Mock PPT
- ✅ 数据结构符合组件 Props

---

#### 子任务 1.1.2: 创建 PPT Page Client 组件 (1 小时)

**文件**: `src/components/admin/ppt/ppts-page-client.tsx`

```typescript
// 新建文件: src/components/admin/ppt/ppts-page-client.tsx

'use client'

import { useState } from 'react'
import { V0PptListTable } from './v0/v0-ppt-list-table'
import { Button } from '@/components/ui/button'
import { useToast } from '@/hooks/use-toast'
import { Upload, Download, Trash2 } from 'lucide-react'
import type { MockPPT } from '@/lib/mock-data/ppts'

interface PptsPageClientProps {
  initialPpts: MockPPT[]
}

export function PptsPageClient({ initialPpts }: PptsPageClientProps) {
  const [ppts, setPpts] = useState(initialPpts)
  const [selectedIds, setSelectedIds] = useState<string[]>([])
  const { toast } = useToast()

  const handleDelete = (id: string) => {
    // Mock 删除逻辑
    setPpts((prev) => prev.filter((p) => p.id !== id))
    setSelectedIds((prev) => prev.filter((sid) => sid !== id))

    toast({
      title: '删除成功',
      description: `已删除 PPT: ${id}`,
    })
  }

  const handleBatchDelete = () => {
    if (selectedIds.length === 0) {
      toast({
        variant: 'destructive',
        title: '请选择要删除的项',
      })
      return
    }

    setPpts((prev) => prev.filter((p) => !selectedIds.includes(p.id)))
    const count = selectedIds.length
    setSelectedIds([])

    toast({
      title: '批量删除成功',
      description: `已删除 ${count} 个 PPT`,
    })
  }

  const handleUpload = () => {
    toast({
      title: '上传功能',
      description: '这是 Mock 功能，真实上传需要后续实现',
    })
  }

  const handleExport = () => {
    toast({
      title: '导出功能',
      description: `正在导出 ${ppts.length} 个 PPT 数据...`,
    })
  }

  return (
    <div className="space-y-4">
      {/* 工具栏 */}
      <div className="flex items-center justify-between">
        <div className="flex items-center gap-2">
          <h2 className="text-lg font-semibold">
            共 {ppts.length} 个 PPT
          </h2>
          {selectedIds.length > 0 && (
            <span className="text-sm text-muted-foreground">
              | 已选择 {selectedIds.length} 个
            </span>
          )}
        </div>
        <div className="flex items-center gap-2">
          {selectedIds.length > 0 && (
            <Button
              variant="destructive"
              size="sm"
              onClick={handleBatchDelete}
            >
              <Trash2 className="mr-2 h-4 w-4" />
              批量删除
            </Button>
          )}
          <Button variant="outline" size="sm" onClick={handleExport}>
            <Download className="mr-2 h-4 w-4" />
            导出
          </Button>
          <Button size="sm" onClick={handleUpload}>
            <Upload className="mr-2 h-4 w-4" />
            上传 PPT
          </Button>
        </div>
      </div>

      {/* PPT 列表表格 */}
      <V0PptListTable
        ppts={ppts}
        selectedIds={selectedIds}
        onSelectionChange={setSelectedIds}
        onDelete={handleDelete}
      />
    </div>
  )
}
```

**验收标准**:
- ✅ 'use client' 标记
- ✅ useState 管理状态
- ✅ Toast 反馈正常
- ✅ 批量操作功能正常
- ✅ Props 类型正确

---

#### 子任务 1.1.3: 修改 Page 使用 AdminPageShell (30 分钟)

**文件**: `src/app/[locale]/(protected)/admin/ppts-v0/page.tsx`

```typescript
// 修改文件: src/app/[locale]/(protected)/admin/ppts-v0/page.tsx

import { AdminPageShell } from '@/components/admin/admin-page-shell'
import { PptsPageClient } from '@/components/admin/ppt/ppts-page-client'
import { MOCK_PPTS } from '@/lib/mock-data/ppts'

export default async function PptsV0Page() {
  // 使用 Mock 数据
  const ppts = MOCK_PPTS

  return (
    <AdminPageShell
      title="PPT 管理"
      description="管理所有 PPT 模板，支持上传、审核、统计和下载"
    >
      <PptsPageClient initialPpts={ppts} />
    </AdminPageShell>
  )
}
```

**验收标准**:
- ✅ 使用 AdminPageShell
- ✅ title 和 description 明确
- ✅ 导入 MOCK_PPTS
- ✅ 传递数据给 Client 组件
- ✅ 符合 mk-saas 规范

---

### Task 1.2: 修复 `/admin/users-v0` 页面

**当前状态**: ❌ 违反 3 项规范
- 未使用 AdminPageShell
- Server Component 无交互
- 需要 Client 分离

**目标**: ✅ 100% 符合 mk-saas 设计标准

---

#### 子任务 1.2.1: 创建 Users Mock 数据 (30 分钟)

**文件**: `src/lib/mock-data/users.ts`

```typescript
// 新建文件: src/lib/mock-data/users.ts

export interface MockUser {
  id: string
  name: string
  email: string
  role: 'user' | 'admin'
  banned: boolean
  banReason?: string
  banExpires?: string
  createdAt: string
  currentCredits?: number
  totalPayments?: number
}

export function generateMockUsers(count: number): MockUser[] {
  const roles = ['user', 'admin'] as const

  return Array.from({ length: count }, (_, i) => ({
    id: `user_${String(i + 1).padStart(3, '0')}`,
    name: `用户 ${i + 1}`,
    email: `user${i + 1}@example.com`,
    role: i < 5 ? 'admin' : 'user',
    banned: i % 10 === 0,
    banReason: i % 10 === 0 ? '违反平台规则' : undefined,
    banExpires: i % 10 === 0
      ? new Date(Date.now() + 7 * 24 * 60 * 60 * 1000).toISOString()
      : undefined,
    createdAt: new Date(Date.now() - Math.random() * 180 * 24 * 60 * 60 * 1000).toISOString(),
    currentCredits: Math.floor(Math.random() * 1000),
    totalPayments: Math.floor(Math.random() * 10000),
  }))
}

// 导出预生成的 Mock 数据
export const MOCK_USERS = generateMockUsers(100)

// 按角色分组
export const MOCK_USERS_BY_ROLE = MOCK_USERS.reduce((acc, user) => {
  if (!acc[user.role]) acc[user.role] = []
  acc[user.role].push(user)
  return acc
}, {} as Record<string, MockUser[]>)

// 按封禁状态分组
export const MOCK_BANNED_USERS = MOCK_USERS.filter((u) => u.banned)
export const MOCK_ACTIVE_USERS = MOCK_USERS.filter((u) => !u.banned)
```

**验收标准**:
- ✅ 文件创建成功
- ✅ TypeScript 类型无错误
- ✅ 生成 100 个 Mock 用户
- ✅ 数据结构符合组件 Props

---

#### 子任务 1.2.2: 创建 Users Page Client 组件 (1 小时)

**文件**: `src/components/admin/users/users-page-client.tsx`

```typescript
// 新建文件: src/components/admin/users/users-page-client.tsx

'use client'

import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { useToast } from '@/hooks/use-toast'
import { UserPlus, Ban, Coins, Download } from 'lucide-react'
import type { MockUser } from '@/lib/mock-data/users'

interface UsersPageClientProps {
  initialUsers: MockUser[]
}

export function UsersPageClient({ initialUsers }: UsersPageClientProps) {
  const [users, setUsers] = useState(initialUsers)
  const [selectedIds, setSelectedIds] = useState<string[]>([])
  const { toast } = useToast()

  const handleBan = (userId: string) => {
    setUsers((prev) =>
      prev.map((u) =>
        u.id === userId
          ? {
              ...u,
              banned: !u.banned,
              banReason: !u.banned ? 'Manual ban' : undefined,
            }
          : u
      )
    )

    const user = users.find((u) => u.id === userId)
    toast({
      title: user?.banned ? '解除封禁成功' : '封禁成功',
      description: `用户 ${user?.name} 状态已更新`,
    })
  }

  const handleAdjustCredits = (userId: string, amount: number) => {
    setUsers((prev) =>
      prev.map((u) =>
        u.id === userId
          ? { ...u, currentCredits: (u.currentCredits || 0) + amount }
          : u
      )
    )

    toast({
      title: '积分调整成功',
      description: `${amount > 0 ? '+' : ''}${amount} 积分`,
    })
  }

  const handleExport = () => {
    toast({
      title: '导出功能',
      description: `正在导出 ${users.length} 个用户数据...`,
    })
  }

  const handleAddUser = () => {
    toast({
      title: '添加用户',
      description: '这是 Mock 功能，真实添加需要后续实现',
    })
  }

  return (
    <div className="space-y-4">
      {/* 工具栏 */}
      <div className="flex items-center justify-between">
        <div className="flex items-center gap-2">
          <h2 className="text-lg font-semibold">
            共 {users.length} 个用户
          </h2>
          <span className="text-sm text-muted-foreground">
            | {users.filter((u) => u.role === 'admin').length} 个管理员
          </span>
          <span className="text-sm text-muted-foreground">
            | {users.filter((u) => u.banned).length} 个被封禁
          </span>
        </div>
        <div className="flex items-center gap-2">
          <Button variant="outline" size="sm" onClick={handleExport}>
            <Download className="mr-2 h-4 w-4" />
            导出
          </Button>
          <Button size="sm" onClick={handleAddUser}>
            <UserPlus className="mr-2 h-4 w-4" />
            添加用户
          </Button>
        </div>
      </div>

      {/* 用户列表 - 这里可以使用现有的表格组件或创建新的 */}
      <div className="rounded-md border">
        <div className="p-4 text-sm text-muted-foreground">
          用户列表表格组件 (待集成具体表格组件)
        </div>
      </div>

      {/* Mock 操作区域 */}
      <div className="grid grid-cols-3 gap-4">
        <Button
          variant="outline"
          onClick={() => handleBan(users[0]?.id)}
        >
          <Ban className="mr-2 h-4 w-4" />
          测试封禁/解封
        </Button>
        <Button
          variant="outline"
          onClick={() => handleAdjustCredits(users[0]?.id, 100)}
        >
          <Coins className="mr-2 h-4 w-4" />
          测试加积分 +100
        </Button>
        <Button
          variant="outline"
          onClick={() => handleAdjustCredits(users[0]?.id, -50)}
        >
          <Coins className="mr-2 h-4 w-4" />
          测试减积分 -50
        </Button>
      </div>
    </div>
  )
}
```

**验收标准**:
- ✅ 'use client' 标记
- ✅ 封禁/解封功能正常
- ✅ 积分调整功能正常
- ✅ Toast 反馈正常
- ✅ Props 类型正确

---

#### 子任务 1.2.3: 修改 Page 使用 AdminPageShell (30 分钟)

**文件**: `src/app/[locale]/(protected)/admin/users-v0/page.tsx`

```typescript
// 修改文件: src/app/[locale]/(protected)/admin/users-v0/page.tsx

import { AdminPageShell } from '@/components/admin/admin-page-shell'
import { UsersPageClient } from '@/components/admin/users/users-page-client'
import { MOCK_USERS } from '@/lib/mock-data/users'

export default async function UsersV0Page() {
  // 使用 Mock 数据
  const users = MOCK_USERS

  return (
    <AdminPageShell
      title="用户管理"
      description="管理所有用户，支持封禁、积分调整、角色管理"
    >
      <UsersPageClient initialUsers={users} />
    </AdminPageShell>
  )
}
```

**验收标准**:
- ✅ 使用 AdminPageShell
- ✅ title 和 description 明确
- ✅ 导入 MOCK_USERS
- ✅ 传递数据给 Client 组件
- ✅ 符合 mk-saas 规范

---

## 🟡 **阶段 2: 中优先级改进**

### Task 2.1: 添加表单 Actions (30 分钟)

#### 子任务 2.1.1: 创建联系表单 Action (15 分钟)

**文件**: `src/actions/marketing/submit-contact.ts`

```typescript
// 新建文件: src/actions/marketing/submit-contact.ts

'use server'

import { z } from 'zod'

const contactSchema = z.object({
  name: z.string().min(2, '名字至少2个字符'),
  email: z.string().email('邮箱格式不正确'),
  message: z.string().min(10, '留言至少10个字符'),
})

export async function submitContactAction(formData: FormData) {
  try {
    const data = {
      name: formData.get('name')?.toString() || '',
      email: formData.get('email')?.toString() || '',
      message: formData.get('message')?.toString() || '',
    }

    // Zod 验证
    const validated = contactSchema.parse(data)

    // Mock 处理 - 记录日志
    console.log('[Mock Contact] Received:', validated)

    // 模拟延迟
    await new Promise((resolve) => setTimeout(resolve, 500))

    return {
      success: true,
      message: '感谢您的留言！我们会尽快回复。'
    }
  } catch (error) {
    if (error instanceof z.ZodError) {
      return {
        success: false,
        message: error.errors[0].message
      }
    }
    return {
      success: false,
      message: '提交失败，请稍后重试'
    }
  }
}
```

**验收标准**:
- ✅ 'use server' 标记
- ✅ Zod 验证正常
- ✅ 返回类型明确
- ✅ 错误处理完整

---

#### 子任务 2.1.2: 创建等候列表 Action (15 分钟)

**文件**: `src/actions/marketing/join-waitlist.ts`

```typescript
// 新建文件: src/actions/marketing/join-waitlist.ts

'use server'

import { z } from 'zod'

const waitlistSchema = z.object({
  email: z.string().email('邮箱格式不正确'),
  name: z.string().min(2, '名字至少2个字符').optional(),
})

export async function joinWaitlistAction(formData: FormData) {
  try {
    const data = {
      email: formData.get('email')?.toString() || '',
      name: formData.get('name')?.toString(),
    }

    // Zod 验证
    const validated = waitlistSchema.parse(data)

    // Mock 处理 - 记录日志
    console.log('[Mock Waitlist] Added:', validated)

    // 模拟延迟
    await new Promise((resolve) => setTimeout(resolve, 500))

    return {
      success: true,
      message: '成功加入等候列表！我们会通过邮件通知您。'
    }
  } catch (error) {
    if (error instanceof z.ZodError) {
      return {
        success: false,
        message: error.errors[0].message
      }
    }
    return {
      success: false,
      message: '加入失败，请稍后重试'
    }
  }
}
```

**验收标准**:
- ✅ 'use server' 标记
- ✅ Zod 验证正常
- ✅ 返回类型明确
- ✅ 错误处理完整

---

### Task 2.2: 检查 AI 页面 (30 分钟)

#### 子任务 2.2.1: 检查 `/ai/audio` 页面 (15 分钟)

**步骤**:
1. 启动开发服务器: `pnpm dev`
2. 访问: `http://localhost:3005/ai/audio`
3. 检查内容:
   - ✅ 页面可以访问
   - ✅ 有标题和说明
   - ✅ 有功能演示或占位内容
   - ✅ 无 JavaScript 错误

**如果缺少内容**:
- 添加基础 UI 和说明文字
- 添加 "功能开发中" 提示

---

#### 子任务 2.2.2: 检查 `/ai/video` 页面 (15 分钟)

**步骤**:
1. 访问: `http://localhost:3005/ai/video`
2. 检查内容 (同上)

**如果缺少内容**:
- 添加基础 UI 和说明文字
- 添加 "功能开发中" 提示

---

### Task 2.3: 验证所有 Admin 页面规范 (30 分钟)

**检查清单**:

```bash
检查文件:
[ ] src/app/[locale]/(protected)/admin/ppts-v0/page.tsx
[ ] src/app/[locale]/(protected)/admin/users-v0/page.tsx
[ ] src/app/[locale]/(protected)/admin/users/page.tsx

验证项:
✅ 使用 AdminPageShell
✅ 有 title 和 description
✅ 数据通过 Props 传递
✅ 交互在 Client Component 中
✅ 符合 mk-saas 规范
```

---

## 🟢 **阶段 3: 全面测试验证**

### Task 3.1: 页面可访问性测试 (20 分钟)

**测试脚本**:

```bash
# 启动开发服务器
pnpm dev

# 测试所有页面路径
测试页面列表:

Marketing (21 个):
✅ /
✅ /about
✅ /contact
✅ /pricing
✅ /roadmap
✅ /changelog
✅ /waitlist
✅ /test
✅ /magicui
✅ /terms
✅ /privacy
✅ /cookie
✅ /blog
✅ /blog/[...slug]
✅ /ai/image
✅ /ai/text
✅ /ai/chat
✅ /ai/audio
✅ /ai/video
✅ /ppt
✅ /ppt/categories
✅ /ppt/category/[name]

Protected (11 个):
✅ /dashboard
✅ /settings/profile
✅ /settings/account
✅ /settings/billing
✅ /settings/credits
✅ /settings/security
✅ /settings/notifications
✅ /admin/users
✅ /admin/users-v0
✅ /admin/ppts-v0
✅ /payment

Auth (5 个):
✅ /auth/login
✅ /auth/register
✅ /auth/forgot-password
✅ /auth/reset-password
✅ /auth/error

Docs (1 个):
✅ /docs

Other (1 个):
✅ /404 (catch-all)
```

**验收标准**:
- ✅ 所有页面返回 200 状态码
- ✅ 没有 404 错误
- ✅ 没有 500 错误
- ✅ 没有空白页面

---

### Task 3.2: Mock 数据和交互测试 (20 分钟)

**测试清单**:

```bash
PPT 管理页面 (/admin/ppts-v0):
✅ 显示 50 个 Mock PPT
✅ 批量选择功能正常
✅ 删除功能有 Toast 反馈
✅ 批量删除功能正常
✅ 上传按钮有反馈
✅ 导出按钮有反馈

用户管理页面 (/admin/users-v0):
✅ 显示 100 个 Mock 用户
✅ 封禁/解封功能正常
✅ 积分调整功能正常
✅ Toast 反馈正常
✅ 导出按钮有反馈

PPT 前台页面:
✅ /ppt 搜索功能正常
✅ /ppt 筛选功能正常
✅ /ppt/categories 分类展示正常
✅ /ppt/category/[name] 动态数据加载
✅ 分页功能正常

表单页面:
✅ /contact 表单提交有反馈
✅ /waitlist 表单提交有反馈
✅ 表单验证正常
```

**验收标准**:
- ✅ Mock 数据正常显示
- ✅ 所有交互有反馈
- ✅ Toast 消息正确
- ✅ 无 Console 错误

---

### Task 3.3: 规范符合度验证 (20 分钟)

**检查清单**:

```bash
布局规范:
✅ Marketing 页面继承 (marketing)/layout.tsx
✅ Protected 页面继承 (protected)/layout.tsx
✅ 没有重复的 Navbar/Footer/Sidebar
✅ 没有自定义 <html>/<body>

Admin 页面规范:
✅ 所有 Admin 页面使用 AdminPageShell
✅ AdminPageShell 包含 title 和 description
✅ 页面只渲染主组件
✅ Client 交互分离到独立组件

组件规范:
✅ 文件命名: kebab-case
✅ 组件导出: PascalCase
✅ 'use client' 标记清晰
✅ Props 接口类型定义

数据访问规范:
✅ 禁止组件中直接 fetch/axios
✅ 使用 Mock 数据或 Server Action
✅ 权限检查通过 useSession (如需要)

UI 组件规范:
✅ 使用 @/components/ui/*
✅ 使用 Tailwind utilities
✅ 使用 cn() helper
✅ 遵循设计系统变量

国际化规范:
✅ 使用 useTranslations hook
✅ 文案在翻译文件中
✅ 无硬编码文本
```

**验收标准**:
- ✅ 100% 符合 mk-saas 规范
- ✅ 无违反设计标准的代码
- ✅ TypeScript 无错误
- ✅ Lint 无警告

---

## 📊 **进度跟踪表**

### 文件创建清单

```bash
新建文件 (7 个):

Mock 数据:
[ ] src/lib/mock-data/ppts.ts
[ ] src/lib/mock-data/users.ts

Client 组件:
[ ] src/components/admin/ppt/ppts-page-client.tsx
[ ] src/components/admin/users/users-page-client.tsx

Server Actions:
[ ] src/actions/marketing/submit-contact.ts
[ ] src/actions/marketing/join-waitlist.ts

测试:
[ ] 创建测试记录文档
```

### 修改文件清单

```bash
修改文件 (2 个):

Admin 页面:
[ ] src/app/[locale]/(protected)/admin/ppts-v0/page.tsx
[ ] src/app/[locale]/(protected)/admin/users-v0/page.tsx
```

---

## ✅ **最终验收标准**

### 页面完成度
```
总页面数: 39

✅ 可访问:    39/39  (100%)
✅ 符合规范:  39/39  (100%)
✅ Mock 交互: 100%
```

### 规范符合度
```
✅ 布局继承:      100%
✅ AdminPageShell: 100% (所有 Admin 页面)
✅ 数据访问:      100% (Mock/Action)
✅ 组件规范:      100%
✅ 国际化:        100%
```

### 功能完整度
```
✅ 所有页面可预览
✅ 所有 Mock 数据正常
✅ 所有交互功能正常
✅ 所有表单有反馈
✅ 无 Console 错误
```

---

## 🚀 **执行建议**

### 推荐执行顺序

```bash
第 1 天上午 (4 小时):
✅ 完成阶段 1 - 高优先级修复
   - Task 1.1: PPT 管理页面 (2h)
   - Task 1.2: 用户管理页面 (2h)

第 1 天下午 (2.5 小时):
✅ 完成阶段 2 - 中优先级改进
   - Task 2.1: 表单 Actions (30min)
   - Task 2.2: AI 页面检查 (30min)
   - Task 2.3: Admin 规范验证 (30min)
✅ 完成阶段 3 - 全面测试
   - Task 3.1-3.3: 测试验证 (1h)
```

### 快速启动

```bash
# 1. 创建目录
mkdir -p src/lib/mock-data
mkdir -p src/components/admin/ppt
mkdir -p src/components/admin/users
mkdir -p src/actions/marketing

# 2. 启动开发服务器
pnpm dev

# 3. 开始创建文件 (按顺序)
# 先创建 Mock 数据
# 再创建 Client 组件
# 最后修改 Page 文件
```

---

## 📝 **任务追踪模板**

```markdown
## 执行记录

### 2025-11-26

#### 阶段 1: 高优先级修复
- [ ] Task 1.1: PPT 管理页面
  - [ ] 1.1.1: Mock 数据 (30min)
  - [ ] 1.1.2: Client 组件 (1h)
  - [ ] 1.1.3: 修改 Page (30min)
- [ ] Task 1.2: 用户管理页面
  - [ ] 1.2.1: Mock 数据 (30min)
  - [ ] 1.2.2: Client 组件 (1h)
  - [ ] 1.2.3: 修改 Page (30min)

#### 阶段 2: 中优先级改进
- [ ] Task 2.1: 表单 Actions (30min)
- [ ] Task 2.2: AI 页面检查 (30min)
- [ ] Task 2.3: Admin 规范验证 (30min)

#### 阶段 3: 全面测试
- [ ] Task 3.1: 可访问性测试 (20min)
- [ ] Task 3.2: Mock 交互测试 (20min)
- [ ] Task 3.3: 规范验证 (20min)

### 问题记录
-

### 完成时间
- 开始:
- 结束:
- 总耗时:
```

---

**文档创建时间**: 2025-11-26
**预计完成时间**: 1 个工作日
**总预计工时**: 6.5 小时

