# 问题10: 站点的风格是什么，前端使用什么？如果要迁移需要遵循什么原则？

## 站点风格

### 1. 设计风格

**整体风格**: 现代化、简洁、专业的 SaaS 产品风格

**设计特点**:
- ✨ 极简主义设计
- 🎨 明暗双主题切换
- 📱 完全响应式布局
- ♿ 无障碍设计（Accessibility）
- 🎭 优雅的动画和过渡效果

---

## 前端技术栈

### 1. 核心框架

```json
{
  "框架": "Next.js 15",
  "UI库": "React 19",
  "语言": "TypeScript 5.8",
  "样式": "TailwindCSS 4",
  "组件库": "Radix UI"
}
```

### 2. UI 组件系统

**Radix UI** (30+ 组件)

**核心组件**:
```typescript
// src/components/ui/
├── accordion.tsx         // 手风琴
├── alert.tsx            // 警告提示
├── alert-dialog.tsx     // 确认对话框
├── avatar.tsx           // 头像
├── badge.tsx            // 徽章
├── button.tsx           // 按钮
├── card.tsx             // 卡片
├── checkbox.tsx         // 复选框
├── dialog.tsx           // 对话框
├── dropdown-menu.tsx    // 下拉菜单
├── form.tsx             // 表单
├── input.tsx            // 输入框
├── label.tsx            // 标签
├── popover.tsx          // 弹出层
├── radio-group.tsx      // 单选组
├── select.tsx           // 选择器
├── separator.tsx        // 分隔线
├── sheet.tsx            // 抽屉
├── skeleton.tsx         // 骨架屏
├── slider.tsx           // 滑块
├── switch.tsx           // 开关
├── table.tsx            // 表格
├── tabs.tsx             // 标签页
├── textarea.tsx         // 文本域
├── toast.tsx            // 提示消息
├── tooltip.tsx          // 工具提示
└── ...
```

**组件特性**:
- ✅ 基于 Radix Primitives（无障碍）
- ✅ 使用 CVA（Class Variance Authority）管理变体
- ✅ Tailwind CSS 样式
- ✅ 完全可定制
- ✅ TypeScript 类型支持

**示例组件**:

```typescript
// src/components/ui/button.tsx
import { cva, type VariantProps } from 'class-variance-authority';

const buttonVariants = cva(
  'inline-flex items-center justify-center rounded-md text-sm font-medium transition-colors',
  {
    variants: {
      variant: {
        default: 'bg-primary text-primary-foreground hover:bg-primary/90',
        destructive: 'bg-destructive text-destructive-foreground hover:bg-destructive/90',
        outline: 'border border-input hover:bg-accent hover:text-accent-foreground',
        ghost: 'hover:bg-accent hover:text-accent-foreground',
      },
      size: {
        default: 'h-10 px-4 py-2',
        sm: 'h-9 rounded-md px-3',
        lg: 'h-11 rounded-md px-8',
        icon: 'h-10 w-10',
      },
    },
    defaultVariants: {
      variant: 'default',
      size: 'default',
    },
  }
);
```

### 3. 样式系统

**TailwindCSS 4**

**配置** (`tailwind.config.ts`):

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  darkMode: 'class',  // 类名切换暗黑模式
  content: [
    './src/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // 设计令牌（Design Tokens）
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        // ... 更多颜色
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
    },
  },
};
```

**CSS 变量** (`src/styles/globals.css`):

```css
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --primary: 222.2 47.4% 11.2%;
    --primary-foreground: 210 40% 98%;
    /* ... 更多变量 */
  }

  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
    /* ... 暗黑模式变量 */
  }
}
```

### 4. 图标系统

**Lucide React** (1000+ 图标)

```typescript
import { Mail, User, Settings, ChevronRight } from 'lucide-react';

// 使用
<Mail className="h-4 w-4" />
<User className="h-5 w-5 text-blue-600" />
<Settings className="h-6 w-6 hover:rotate-90 transition-transform" />
```

**Tabler Icons** (5000+ 图标)

```typescript
import { IconBrandGithub, IconMail } from '@tabler/icons-react';

<IconBrandGithub size={24} />
<IconMail stroke={1.5} />
```

### 5. 动画和过渡

**Framer Motion** (未默认安装，可选)

```bash
pnpm add framer-motion
```

```typescript
import { motion } from 'framer-motion';

<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.5 }}
>
  内容
</motion.div>
```

**Tailwind 内置动画**:

```tsx
<div className="animate-fade-in">淡入</div>
<div className="animate-slide-up">向上滑动</div>
<div className="animate-spin">旋转</div>
```

---

## 主题系统

### 1. 主题配置

**文件位置**: `src/config/website.tsx`

```typescript
ui: {
  theme: {
    defaultTheme: 'default',  // 默认主题
    enableSwitch: true,       // 允许切换主题
  },
  mode: {
    defaultMode: 'dark',      // 默认模式（light/dark）
    enableSwitch: true,       // 允许切换明暗模式
  },
}
```

### 2. 主题切换实现

**提供者** (`src/components/theme-provider.tsx`):

```typescript
'use client';

import { ThemeProvider as NextThemesProvider } from 'next-themes';

export function ThemeProvider({ children, ...props }) {
  return (
    <NextThemesProvider
      attribute="class"
      defaultTheme="system"
      enableSystem
      disableTransitionOnChange
      {...props}
    >
      {children}
    </NextThemesProvider>
  );
}
```

**主题切换器**:

```typescript
'use client';

import { Moon, Sun } from 'lucide-react';
import { useTheme } from 'next-themes';

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <button
      onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}
      className="rounded-md p-2 hover:bg-accent"
    >
      <Sun className="h-5 w-5 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
      <Moon className="absolute h-5 w-5 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
      <span className="sr-only">切换主题</span>
    </button>
  );
}
```

---

## 布局系统

### 1. 页面布局

**标准布局结构**:

```
┌─────────────────────────────────────┐
│          导航栏 (Navbar)              │
├─────────────────────────────────────┤
│                                     │
│          主要内容区                   │
│      (Main Content Area)            │
│                                     │
│   ┌──────────────┬─────────────┐   │
│   │              │             │   │
│   │   内容       │   侧边栏    │   │
│   │  (Content)  │  (Sidebar)  │   │
│   │              │             │   │
│   └──────────────┴─────────────┘   │
│                                     │
├─────────────────────────────────────┤
│          页脚 (Footer)               │
└─────────────────────────────────────┘
```

**实现** (`src/app/[locale]/layout.tsx`):

```typescript
export default function Layout({ children }) {
  return (
    <div className="flex min-h-screen flex-col">
      {/* 导航栏 */}
      <Navbar />

      {/* 主要内容 */}
      <main className="flex-1 container mx-auto px-4 py-8">
        {children}
      </main>

      {/* 页脚 */}
      <Footer />
    </div>
  );
}
```

### 2. 响应式设计

**断点系统** (Tailwind 默认):

```
sm:  640px   // 手机横屏
md:  768px   // 平板
lg:  1024px  // 笔记本
xl:  1280px  // 桌面
2xl: 1536px  // 大屏
```

**响应式示例**:

```tsx
<div className="
  grid
  grid-cols-1      // 移动端：1列
  md:grid-cols-2   // 平板：2列
  lg:grid-cols-3   // 桌面：3列
  gap-4            // 间距
">
  {/* 内容 */}
</div>
```

---

## 迁移原则

### 原则1: 保持设计令牌一致

**步骤**:

1. **提取旧站的颜色**:
```typescript
// 旧站颜色
const oldColors = {
  primary: '#3b82f6',
  secondary: '#10b981',
  background: '#ffffff',
  text: '#1f2937',
};
```

2. **转换为 CSS 变量**:
```css
/* src/styles/globals.css */
:root {
  --primary: 221 83% 53%;        /* #3b82f6 转换为 HSL */
  --secondary: 142 71% 45%;      /* #10b981 转换为 HSL */
  --background: 0 0% 100%;
  --foreground: 220 17% 17%;
}
```

3. **更新 Tailwind 配置**:
```typescript
// tailwind.config.ts
colors: {
  primary: 'hsl(var(--primary))',
  secondary: 'hsl(var(--secondary))',
}
```

### 原则2: 渐进式迁移组件

**迁移顺序**:

```
第1阶段: 基础组件
├─ Button
├─ Input
├─ Card
└─ Link

第2阶段: 布局组件
├─ Navbar
├─ Footer
├─ Sidebar
└─ Container

第3阶段: 复杂组件
├─ Form
├─ Table
├─ Modal
└─ Dropdown

第4阶段: 页面组件
├─ BlogList
├─ BlogPost
├─ Homepage
└─ Dashboard
```

**组件迁移示例**:

```typescript
// 旧组件（原站）
function OldButton({ children, onClick, className }) {
  return (
    <button
      className={`btn ${className}`}
      onClick={onClick}
    >
      {children}
    </button>
  );
}

// 新组件（mksaas-blog）
import { Button } from '@/components/ui/button';

// 保持相同的 API
function MigratedButton({ children, onClick, className }) {
  return (
    <Button
      onClick={onClick}
      className={className}
    >
      {children}
    </Button>
  );
}
```

### 原则3: 响应式优先

**移动端优先的开发流程**:

```tsx
// ❌ 错误：桌面优先
<div className="w-96 md:w-64">  // 从大到小

// ✅ 正确：移动优先
<div className="w-full md:w-64 lg:w-96">  // 从小到大
```

### 原则4: 保持可访问性

**无障碍检查清单**:

```tsx
// ✅ 语义化 HTML
<nav>...</nav>
<main>...</main>
<article>...</article>

// ✅ ARIA 属性
<button aria-label="关闭对话框">
  <X />
</button>

// ✅ 键盘导航
<a href="/blog" className="focus:ring-2 focus:ring-primary">
  博客
</a>

// ✅ 颜色对比度
// 确保文字和背景的对比度至少为 4.5:1
```

### 原则5: 性能优化

**图片优化**:

```tsx
// ✅ 使用 Next.js Image 组件
import Image from 'next/image';

<Image
  src="/blog-post.jpg"
  alt="文章标题"
  width={800}
  height={600}
  loading="lazy"
  placeholder="blur"
/>
```

**代码分割**:

```tsx
// ✅ 动态导入大组件
import dynamic from 'next/dynamic';

const HeavyComponent = dynamic(() => import('@/components/heavy'), {
  loading: () => <Skeleton />,
  ssr: false,  // 仅客户端渲染
});
```

### 原则6: 样式隔离

**避免全局样式污染**:

```tsx
// ❌ 全局样式
.button {
  background: blue;
}

// ✅ 组件内样式（Tailwind）
<button className="bg-blue-600 hover:bg-blue-700">
  按钮
</button>

// ✅ CSS Modules（如果需要）
import styles from './button.module.css';

<button className={styles.button}>
  按钮
</button>
```

### 原则7: TypeScript 类型安全

**组件类型定义**:

```typescript
// ✅ 明确的 Props 类型
interface ButtonProps {
  children: React.ReactNode;
  variant?: 'default' | 'outline' | 'ghost';
  size?: 'sm' | 'default' | 'lg';
  onClick?: () => void;
  disabled?: boolean;
}

export function Button({
  children,
  variant = 'default',
  size = 'default',
  onClick,
  disabled = false,
}: ButtonProps) {
  // ...
}
```

---

## 设计系统迁移工具

### 1. Figma to Tailwind

使用 Figma 插件转换设计为 Tailwind 类名。

### 2. CSS to Tailwind 转换器

```bash
# 在线工具
https://transform.tools/css-to-tailwind
```

### 3. 颜色转换工具

```typescript
// 工具函数：Hex to HSL
function hexToHSL(hex: string): string {
  // 转换逻辑
  return 'hsl(...)';
}
```

---

## 迁移检查清单

### ✅ 设计令牌

- [ ] 提取旧站颜色系统
- [ ] 转换为 CSS 变量
- [ ] 定义明暗主题
- [ ] 配置 Tailwind

### ✅ 组件迁移

- [ ] 审计所有现有组件
- [ ] 映射到 Radix UI 组件
- [ ] 保持组件 API 一致
- [ ] 添加 TypeScript 类型

### ✅ 布局和响应式

- [ ] 实现响应式布局
- [ ] 移动端优先开发
- [ ] 测试各种屏幕尺寸
- [ ] 优化平板显示

### ✅ 无障碍

- [ ] 添加 ARIA 属性
- [ ] 支持键盘导航
- [ ] 确保颜色对比度
- [ ] 测试屏幕阅读器

### ✅ 性能

- [ ] 优化图片加载
- [ ] 实现代码分割
- [ ] 延迟加载组件
- [ ] 测试 Core Web Vitals

---

## 总结

**站点风格**:
- 现代化、简洁、专业的 SaaS 设计
- 完整的明暗主题支持
- 响应式和无障碍设计

**前端技术**:
- Next.js 15 + React 19
- TailwindCSS 4 + Radix UI
- TypeScript 完整类型支持
- 组件化和模块化架构

**迁移原则**:
1. 保持设计令牌一致
2. 渐进式迁移组件
3. 响应式优先
4. 保持可访问性
5. 性能优化
6. 样式隔离
7. TypeScript 类型安全

**推荐流程**:
1. 提取设计令牌（颜色、字体、间距）
2. 设置主题系统
3. 逐个迁移组件
4. 测试和优化
5. 部署验证

mksaas-blog 的前端架构非常灵活和可定制，可以轻松适配各种设计风格！
