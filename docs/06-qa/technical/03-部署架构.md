# 问题3: 现在的工程的设计的部署在什么架构上，Vercel 还是 Cloudflare？

## 部署架构概述

mksaas-blog 项目设计为**多平台兼容**的架构，支持多种部署方式：

### 支持的部署平台

1. **Vercel**（推荐，Next.js 官方平台）
2. **Cloudflare Workers**（通过 OpenNext）
3. **Docker**（自托管）
4. **其他 Node.js 平台**（如 AWS、Railway、Render等）

---

## 1. Vercel 部署（推荐）

### 为什么选择 Vercel？

✅ **Next.js 官方平台**：由 Next.js 开发团队打造
✅ **零配置部署**：连接 GitHub 仓库即可自动部署
✅ **全球 CDN**：自动边缘缓存和分发
✅ **自动 HTTPS**：免费 SSL 证书
✅ **即时回滚**：一键回滚到任何历史版本
✅ **预览部署**：每个 PR 自动生成预览链接
✅ **性能监控**：内置 Analytics 和 Speed Insights
✅ **无服务器函数**：API 路由自动转换为 Serverless Functions

### Vercel 部署配置

项目已针对 Vercel 优化，无需额外配置：

```typescript
// next.config.ts
const nextConfig: NextConfig = {
  // Vercel 会自动识别 Next.js 项目
  images: {
    unoptimized: true, // 避免图片优化配额限制
  },
};
```

### Vercel 部署流程

#### 方式1: 通过 Vercel Dashboard（最简单）

```
1. 访问 https://vercel.com
2. 点击 "New Project"
3. 导入 GitHub 仓库（ameureka/mksaas-blog）
4. Vercel 自动检测 Next.js 项目
5. 配置环境变量（DATABASE_URL、STRIPE_SECRET_KEY等）
6. 点击 "Deploy"
7. 等待构建完成（约 2-5 分钟）
```

#### 方式2: 通过 Vercel CLI

```bash
# 安装 Vercel CLI
npm i -g vercel

# 登录 Vercel
vercel login

# 部署到生产环境
vercel --prod

# 或部署到预览环境
vercel
```

### Vercel 自动部署

连接 GitHub 后，Vercel 会自动：
- **主分支推送** → 自动部署到生产环境
- **Pull Request** → 自动生成预览部署
- **构建失败** → 发送邮件通知

### Vercel 环境变量配置

在 Vercel Dashboard → Settings → Environment Variables 中配置：

```bash
# 数据库
DATABASE_URL=postgresql://...

# 认证
BETTER_AUTH_SECRET=xxx
GITHUB_CLIENT_ID=xxx
GITHUB_CLIENT_SECRET=xxx
GOOGLE_CLIENT_ID=xxx
GOOGLE_CLIENT_SECRET=xxx

# 支付
STRIPE_SECRET_KEY=sk_live_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
NEXT_PUBLIC_STRIPE_PRICE_PRO_MONTHLY=price_xxx

# 邮件
RESEND_API_KEY=re_xxx

# AI
OPENAI_API_KEY=sk-xxx

# 存储
AWS_ACCESS_KEY_ID=xxx
AWS_SECRET_ACCESS_KEY=xxx
AWS_REGION=us-east-1
AWS_BUCKET_NAME=xxx

# 基础 URL
NEXT_PUBLIC_BASE_URL=https://yourdomain.com
```

### Vercel 性能优化

1. **边缘缓存**：静态页面自动缓存在全球 CDN
2. **ISR（增量静态再生）**：可配置页面重新验证
3. **图片优化**：自动优化图片（有配额限制）
4. **压缩**：自动 Gzip/Brotli 压缩

### Vercel 限制

**免费层（Hobby）**：
- ✅ 100 GB 带宽/月
- ✅ 无限部署
- ✅ 自动 HTTPS
- ⚠️ 1000 张图片优化/月（已在配置中禁用）
- ⚠️ Serverless Function 执行时间：10秒
- ⚠️ Serverless Function 内存：1024 MB

**Pro 层（$20/月）**：
- ✅ 1 TB 带宽/月
- ✅ 5000 张图片优化/月
- ✅ Serverless Function 执行时间：60秒
- ✅ Serverless Function 内存：3008 MB
- ✅ 团队协作

---

## 2. Cloudflare Workers 部署

### 为什么选择 Cloudflare？

✅ **全球边缘网络**：在 300+ 数据中心运行
✅ **低延迟**：请求在最近的边缘节点处理
✅ **免费层慷慨**：100,000 请求/天
✅ **无冷启动**：即时响应
✅ **DDoS 防护**：内置 DDoS 保护
✅ **KV 存储**：边缘键值存储

### Cloudflare 部署准备

需要使用 **OpenNext** 适配器将 Next.js 项目转换为 Cloudflare Workers 兼容格式：

```bash
# 安装 OpenNext
npm install -D @opennextjs/cloudflare

# 构建 Cloudflare Workers 版本
npx @opennextjs/cloudflare
```

### Cloudflare 部署流程

#### 方式1: 通过 Wrangler CLI

```bash
# 安装 Wrangler
npm install -g wrangler

# 登录 Cloudflare
wrangler login

# 发布到 Cloudflare Workers
wrangler deploy
```

#### 方式2: 通过 GitHub Actions

创建 `.github/workflows/deploy-cloudflare.yml`:

```yaml
name: Deploy to Cloudflare Workers

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: pnpm install

      - name: Build for Cloudflare
        run: npx @opennextjs/cloudflare

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
```

### Cloudflare Pages 替代方案

也可以使用 **Cloudflare Pages**（更简单）：

```
1. 访问 Cloudflare Dashboard
2. Pages → Create a project
3. 连接 GitHub 仓库
4. 构建命令: pnpm build
5. 输出目录: .next
6. 部署
```

### Cloudflare 环境变量

在 Cloudflare Dashboard → Workers & Pages → Settings 中配置环境变量（与 Vercel 相同）。

### Cloudflare 限制

**免费层（Free）**：
- ✅ 100,000 请求/天
- ✅ 无限带宽
- ✅ KV 存储：100,000 读取/天
- ⚠️ CPU 时间：10ms/请求

**Workers Paid（$5/月起）**：
- ✅ 10,000,000 请求/月
- ✅ CPU 时间：30ms/请求
- ✅ KV 存储无限

---

## 3. Docker 部署（自托管）

### 为什么选择 Docker？

✅ **完全控制**：自己管理服务器和环境
✅ **成本优化**：可以使用廉价 VPS
✅ **数据隐私**：数据完全在自己控制下
✅ **无平台限制**：不受第三方平台限制

### Docker 配置

项目已包含 Docker 支持：

```typescript
// next.config.ts
const nextConfig: NextConfig = {
  // Docker standalone output
  ...(process.env.DOCKER_BUILD === 'true' && { output: 'standalone' }),
};
```

### Docker 部署流程

#### 步骤1: 创建 Dockerfile

```dockerfile
# Dockerfile
FROM node:20-alpine AS base

# Install dependencies
FROM base AS deps
RUN apk add --no-cache libc6-compat
WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install --frozen-lockfile

# Build the app
FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .

ENV DOCKER_BUILD=true
RUN npm install -g pnpm && pnpm build

# Production image
FROM base AS runner
WORKDIR /app

ENV NODE_ENV=production

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

USER nextjs

EXPOSE 3000

ENV PORT=3000

CMD ["node", "server.js"]
```

#### 步骤2: 创建 docker-compose.yml

```yaml
version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      # 其他环境变量...
    depends_on:
      - postgres
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=mksaas
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
```

#### 步骤3: 构建和运行

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

### 推荐的 Docker 托管平台

1. **Dokploy**（推荐，项目博客中有教程）
   - 开源自托管 PaaS
   - 类似 Vercel 的体验
   - 支持自动部署

2. **Railway**
   - 简单易用
   - 自动 HTTPS
   - $5/月起

3. **Render**
   - 免费层可用
   - 自动部署
   - 类似 Heroku

4. **DigitalOcean App Platform**
   - $5/月起
   - 托管数据库
   - 简单部署

5. **AWS ECS/Fargate**
   - 企业级
   - 完全托管
   - 按需付费

---

## 4. 其他部署选项

### 4.1 Netlify

```bash
# 安装 Netlify CLI
npm install -g netlify-cli

# 登录
netlify login

# 部署
netlify deploy --prod
```

### 4.2 AWS Amplify

通过 AWS Amplify Console 连接 GitHub 仓库即可。

### 4.3 自托管 VPS

在任何 VPS（如 DigitalOcean、Linode、Vultr）上：

```bash
# 克隆代码
git clone https://github.com/ameureka/mksaas-blog.git
cd mksaas-blog

# 安装依赖
pnpm install

# 构建
pnpm build

# 使用 PM2 运行
npm install -g pm2
pm2 start pnpm --name "mksaas-blog" -- start

# 配置 Nginx 反向代理
# 设置 HTTPS（Let's Encrypt）
```

---

## 推荐部署方案

### 方案1: Vercel（最简单）

**适用场景**：
- 快速上线
- 个人博客或小型项目
- 不想管理服务器

**优势**：
- 零配置
- 自动 HTTPS
- 全球 CDN
- 免费额度充足

**成本**：
- 免费层：$0/月（适合大多数个人博客）
- Pro 层：$20/月

---

### 方案2: Cloudflare Workers + Pages（性能最优）

**适用场景**：
- 全球访问低延迟
- 大流量网站
- 需要边缘计算

**优势**：
- 全球边缘网络
- 无冷启动
- DDoS 防护
- 免费额度高

**成本**：
- 免费层：$0/月（10万请求/天）
- Paid 层：$5/月起

---

### 方案3: Docker + Dokploy（完全控制）

**适用场景**：
- 需要完全控制
- 多个项目共享服务器
- 数据隐私要求高
- 成本敏感

**优势**：
- 完全控制
- 成本可控
- 私有数据
- 无平台锁定

**成本**：
- VPS：$5-20/月（DigitalOcean、Vultr等）
- Dokploy：免费开源

---

## 部署最佳实践

### 1. 环境变量管理

- ✅ 使用平台的环境变量功能
- ✅ 本地使用 `.env.local`（不提交到 Git）
- ✅ 生产环境使用平台环境变量
- ⚠️ 永远不要将密钥提交到代码仓库

### 2. 数据库托管

推荐使用托管数据库服务：
- **Neon**（推荐）：无服务器 PostgreSQL，免费层可用
- **Supabase**：PostgreSQL + 实时功能
- **PlanetScale**：MySQL 兼容
- **Railway**：PostgreSQL + Redis

### 3. CI/CD 设置

```yaml
# .github/workflows/ci.yml
name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm lint
      - run: pnpm build
```

### 4. 监控和日志

- **Vercel Analytics**：性能监控
- **Sentry**：错误追踪
- **LogRocket**：用户会话记录
- **Better Stack**：日志管理

---

## 当前项目部署状态

根据代码分析，项目当前配置：

```typescript
// next.config.ts 支持多种部署方式：
1. 默认配置适用于 Vercel
2. Docker 模式（DOCKER_BUILD=true）
3. 图片优化已禁用（适配 Vercel 免费层）
```

**推荐部署方案**：
1. **开发/测试**：Vercel 免费层
2. **生产环境**：Vercel Pro 或 Cloudflare Workers
3. **自托管需求**：Docker + Dokploy

---

## 总结

mksaas-blog 支持**多种部署方式**，推荐顺序：

1. **Vercel**（最简单，推荐新手）
2. **Cloudflare Workers**（最快，推荐高流量）
3. **Docker + Dokploy**（最灵活，推荐技术团队）

选择部署平台时考虑：
- 🎯 **简单性**：Vercel > Cloudflare > Docker
- ⚡ **性能**：Cloudflare > Vercel > Docker
- 💰 **成本**：Docker < Cloudflare < Vercel
- 🔧 **控制度**：Docker > Cloudflare > Vercel
