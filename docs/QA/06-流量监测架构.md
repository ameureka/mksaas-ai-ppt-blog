# 问题6: 代码有没有监测网站访问流量的连接，以及架构设计？

## 当前流量监测状态

### 配置概览

**文件位置**: `src/config/website.tsx`

```typescript
analytics: {
  enableVercelAnalytics: false,    // Vercel 分析
  enableSpeedInsights: false,      // 速度洞察
}
```

**当前状态**: ❌ 默认未启用（可通过配置开启）

---

## 1. 已集成的分析工具

### 1.1 Vercel Analytics

**状态**: 代码已准备，需配置启用

**优势**：
- ✅ 零配置集成
- ✅ 实时访客统计
- ✅ 页面浏览量追踪
- ✅ 地理位置分布
- ✅ 设备和浏览器统计
- ✅ 隐私友好（无 Cookie）

**如何启用**：

#### 步骤1: 修改配置

```typescript
// src/config/website.tsx
analytics: {
  enableVercelAnalytics: true,
  enableSpeedInsights: true,
}
```

#### 步骤2: 安装依赖

```bash
pnpm add @vercel/analytics @vercel/speed-insights
```

#### 步骤3: 添加到布局

```typescript
// src/app/layout.tsx
import { Analytics } from '@vercel/analytics/react';
import { SpeedInsights } from '@vercel/speed-insights/next';

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        {websiteConfig.analytics.enableVercelAnalytics && <Analytics />}
        {websiteConfig.analytics.enableSpeedInsights && <SpeedInsights />}
      </body>
    </html>
  );
}
```

**功能**：
- 📊 **Vercel Analytics**: 访问量、页面浏览、用户路径
- ⚡ **Speed Insights**: Web Vitals（LCP、FID、CLS）

**成本**：
- 免费层：2,500 次事件/月
- Pro 层：$10/月，100,000 次事件

### 1.2 OpenPanel（可选）

**配置**：
```typescript
// src/config/website.tsx
NEXT_PUBLIC_OPENPANEL_CLIENT_ID=your_client_id
```

**特点**：
- 开源替代品
- 隐私友好
- 自托管选项

### 1.3 PostHog（可选）

**环境变量**：
```bash
POSTHOG_API_KEY=phc_xxx
```

**特点**：
- 产品分析
- 用户行为追踪
- 功能标志
- A/B 测试

---

## 2. 推荐的流量监测架构

### 架构1: Vercel Analytics（最简单）

```
用户访问页面
    ↓
Next.js 渲染
    ↓
<Analytics /> 组件自动追踪
    ↓
发送到 Vercel Analytics API
    ↓
Vercel Dashboard 展示数据
```

**优势**：
- ✅ 零配置
- ✅ 无需额外服务器
- ✅ 与 Vercel 部署集成
- ✅ 隐私友好

**适用场景**：
- 个人博客
- 小型项目
- 快速上线

---

### 架构2: Google Analytics 4（最全面）

**安装 GA4**：

#### 步骤1: 创建 GA4 属性

访问 [Google Analytics](https://analytics.google.com/)

#### 步骤2: 安装 gtag

```bash
pnpm add react-ga4
```

#### 步骤3: 创建 GA 组件

```typescript
// src/lib/analytics/google-analytics.tsx
'use client';

import { useEffect } from 'react';
import { usePathname, useSearchParams } from 'next/navigation';
import ReactGA from 'react-ga4';

const GA_TRACKING_ID = process.env.NEXT_PUBLIC_GA_TRACKING_ID;

export function GoogleAnalytics() {
  const pathname = usePathname();
  const searchParams = useSearchParams();

  useEffect(() => {
    if (GA_TRACKING_ID) {
      ReactGA.initialize(GA_TRACKING_ID);
    }
  }, []);

  useEffect(() => {
    if (GA_TRACKING_ID && pathname) {
      const url = pathname + searchParams.toString();
      ReactGA.send({ hitType: 'pageview', page: url });
    }
  }, [pathname, searchParams]);

  return null;
}
```

#### 步骤4: 添加到布局

```typescript
// src/app/layout.tsx
import { GoogleAnalytics } from '@/lib/analytics/google-analytics';

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <GoogleAnalytics />
      </body>
    </html>
  );
}
```

#### 步骤5: 配置环境变量

```bash
# .env.local
NEXT_PUBLIC_GA_TRACKING_ID=G-XXXXXXXXXX
```

**GA4 功能**：
- 📊 实时访客统计
- 🗺️ 地理位置分析
- 📱 设备和浏览器分布
- 🔍 搜索关键词
- 🎯 转化漏斗
- 👥 用户行为流
- 📈 自定义事件

**数据流**：
```
用户访问
    ↓
Next.js 页面加载
    ↓
gtag.js 脚本加载
    ↓
发送 pageview 事件
    ↓
Google Analytics 服务器
    ↓
GA4 Dashboard
```

---

### 架构3: Plausible（隐私友好）

**特点**：
- 🔒 完全符合 GDPR
- 🚫 不使用 Cookie
- 📊 简洁的仪表盘
- ⚡ 轻量级脚本（< 1KB）

**安装**：

```bash
pnpm add next-plausible
```

```typescript
// next.config.ts
import withPlausible from 'next-plausible';

export default withPlausible({
  domain: 'yourdomain.com',
  customDomain: 'https://plausible.yourdomain.com', // 可选
})(nextConfig);
```

**使用**：

```tsx
import PlausibleProvider from 'next-plausible';

export default function RootLayout({ children }) {
  return (
    <html>
      <head>
        <PlausibleProvider domain="yourdomain.com" />
      </head>
      <body>{children}</body>
    </html>
  );
}
```

**成本**：
- 自托管：免费
- 托管版：$9/月（10,000 次页面浏览）

---

### 架构4: 自建分析（Umami）

**Umami** 是开源的、自托管的 Google Analytics 替代品。

**部署方式**：

#### 选项1: Vercel 一键部署

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/umami-software/umami)

#### 选项2: Docker 部署

```yaml
# docker-compose.yml
version: '3'
services:
  umami:
    image: ghcr.io/umami-software/umami:postgresql-latest
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://umami:umami@db:5432/umami
      DATABASE_TYPE: postgresql
      APP_SECRET: replace-me-with-random-string
    depends_on:
      - db

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: umami
      POSTGRES_USER: umami
      POSTGRES_PASSWORD: umami
    volumes:
      - umami-db-data:/var/lib/postgresql/data

volumes:
  umami-db-data:
```

```bash
docker-compose up -d
```

**集成到项目**：

```tsx
// src/components/analytics/umami.tsx
'use client';

import Script from 'next/script';

export function UmamiAnalytics() {
  return (
    <Script
      async
      src="https://your-umami-domain.com/script.js"
      data-website-id="your-website-id"
    />
  );
}
```

**Umami 优势**：
- ✅ 完全开源
- ✅ 自托管，数据完全掌控
- ✅ 无 Cookie，符合 GDPR
- ✅ 简洁易用
- ✅ 支持多网站
- ✅ 永久免费

---

## 3. 完整的流量监测架构设计

### 推荐方案：多层次监测

```
┌─────────────────────────────────────────┐
│          用户访问网站                     │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│   第1层：页面浏览和访客统计                │
│   - Vercel Analytics / Umami            │
│   - 基础数据：PV、UV、来源                │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│   第2层：用户行为分析                     │
│   - Google Analytics 4 / PostHog        │
│   - 用户路径、转化漏斗、事件追踪           │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│   第3层：性能监控                         │
│   - Vercel Speed Insights               │
│   - Web Vitals (LCP, FID, CLS)         │
│   - 页面加载时间                          │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│   第4层：错误追踪                         │
│   - Sentry                              │
│   - JavaScript 错误、API 错误            │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│   数据汇总和可视化                        │
│   - Grafana / Metabase                  │
│   - 自定义仪表盘                          │
└─────────────────────────────────────────┘
```

### 实现代码

```typescript
// src/lib/analytics/index.tsx
'use client';

import { useEffect } from 'react';
import { usePathname, useSearchParams } from 'next/navigation';

// 页面浏览追踪
export function usePageViews() {
  const pathname = usePathname();
  const searchParams = useSearchParams();

  useEffect(() => {
    if (pathname) {
      // 发送到多个分析服务
      trackPageView({
        url: pathname + searchParams.toString(),
        title: document.title,
        referrer: document.referrer,
      });
    }
  }, [pathname, searchParams]);
}

// 统一的追踪函数
function trackPageView({ url, title, referrer }) {
  // 1. Vercel Analytics (自动)
  // 无需手动调用

  // 2. Google Analytics
  if (window.gtag) {
    window.gtag('event', 'page_view', {
      page_path: url,
      page_title: title,
      page_referrer: referrer,
    });
  }

  // 3. Umami
  if (window.umami) {
    window.umami.track({ url, title, referrer });
  }

  // 4. 自定义分析服务
  fetch('/api/analytics/pageview', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ url, title, referrer }),
  });
}

// 事件追踪
export function trackEvent(eventName: string, properties?: object) {
  // Google Analytics
  if (window.gtag) {
    window.gtag('event', eventName, properties);
  }

  // Umami
  if (window.umami) {
    window.umami.track(eventName, properties);
  }

  // 自定义
  fetch('/api/analytics/event', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ event: eventName, properties }),
  });
}
```

### 使用示例

```tsx
// 组件中使用
'use client';

import { trackEvent } from '@/lib/analytics';

export function NewsletterForm() {
  const handleSubmit = async (e) => {
    e.preventDefault();

    // 追踪订阅事件
    trackEvent('newsletter_subscribe', {
      source: 'homepage',
      email_domain: email.split('@')[1],
    });

    // 提交表单...
  };

  return <form onSubmit={handleSubmit}>...</form>;
}
```

---

## 4. 自建 API 端点

### 简单的访问日志记录

```typescript
// src/app/api/analytics/pageview/route.ts
import { headers } from 'next/headers';
import { db } from '@/db';
import { pageViews } from '@/db/schema';

export async function POST(request: Request) {
  const { url, title, referrer } = await request.json();
  const headersList = headers();

  // 提取访客信息
  const userAgent = headersList.get('user-agent') || '';
  const ipAddress = headersList.get('x-forwarded-for') || headersList.get('x-real-ip') || '';

  // 保存到数据库
  await db.insert(pageViews).values({
    url,
    title,
    referrer,
    userAgent,
    ipAddress: hashIP(ipAddress), // 哈希 IP 以保护隐私
    createdAt: new Date(),
  });

  return Response.json({ success: true });
}

// 简单的 IP 哈希
function hashIP(ip: string): string {
  // 使用 crypto 模块哈希 IP
  const crypto = require('crypto');
  return crypto.createHash('sha256').update(ip).digest('hex').substring(0, 16);
}
```

### 数据库 Schema

```typescript
// src/db/schema.ts
export const pageViews = pgTable('page_view', {
  id: text('id')
    .primaryKey()
    .$defaultFn(() => nanoid()),
  url: text('url').notNull(),
  title: text('title'),
  referrer: text('referrer'),
  userAgent: text('user_agent'),
  ipAddress: text('ip_address'), // 哈希后的 IP
  createdAt: timestamp('created_at', { mode: 'date' }).notNull().defaultNow(),
});

export const events = pgTable('event', {
  id: text('id')
    .primaryKey()
    .$defaultFn(() => nanoid()),
  eventName: text('event_name').notNull(),
  properties: jsonb('properties'),
  url: text('url'),
  createdAt: timestamp('created_at', { mode: 'date' }).notNull().defaultNow(),
});
```

### 统计查询

```typescript
// src/app/api/analytics/stats/route.ts
export async function GET() {
  const stats = await db
    .select({
      url: pageViews.url,
      count: sql<number>`count(*)`,
    })
    .from(pageViews)
    .groupBy(pageViews.url)
    .orderBy(sql`count(*) desc`)
    .limit(10);

  return Response.json(stats);
}
```

---

## 5. 推荐的完整方案

### 方案1: 最小化（免费）

```
Vercel Analytics（基础统计）
    +
Vercel Speed Insights（性能）
```

**成本**: $0/月（免费层）

---

### 方案2: 开源自托管（完全控制）

```
Umami（访问统计）
    +
Plausible（用户行为）
    +
自建 API（自定义事件）
```

**成本**: 服务器成本（$5-20/月）

---

### 方案3: 专业方案（功能最全）

```
Google Analytics 4（全面分析）
    +
Vercel Speed Insights（性能）
    +
Sentry（错误追踪）
    +
PostHog（产品分析）
```

**成本**: $0-50/月

---

## 6. 实施建议

### 立即实施（第一阶段）

1. **启用 Vercel Analytics**
   ```typescript
   analytics: { enableVercelAnalytics: true }
   ```

2. **添加基础事件追踪**
   - 文章浏览
   - 新闻订阅
   - 按钮点击

### 近期实施（第二阶段）

3. **集成 Google Analytics 4**
   - 更详细的用户行为
   - 转化漏斗

4. **添加性能监控**
   - Vercel Speed Insights
   - Web Vitals 追踪

### 长期优化（第三阶段）

5. **自建分析系统**
   - 完全掌控数据
   - 自定义报表

6. **高级功能**
   - A/B 测试
   - 用户画像
   - 预测分析

---

## 总结

**当前状态**: ⚠️ 已准备但未启用

**推荐行动**:
1. **立即**：启用 Vercel Analytics
2. **1周内**：集成 Google Analytics 4
3. **1个月内**：考虑自建或开源方案

**最佳实践**:
- ✅ 多层次监测（访问、行为、性能、错误）
- ✅ 隐私优先（最小化数据收集）
- ✅ 数据驱动决策
- ✅ 定期审查和优化

项目已经有很好的分析架构基础，只需要启用配置即可！
