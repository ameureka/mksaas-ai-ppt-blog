# 📝 常用命令参考

所有项目的常用 pnpm 命令速查表。

---

## 🚀 开发相关

```bash
# 启动开发服务器
pnpm dev
# 访问 http://localhost:3005
# 热重载: 自动刷新

# 构建生产版本
pnpm build
# 输出: .next/ 目录

# 启动生产服务器
pnpm start
# 用于本地测试生产版本
```

---

## 🗄️ 数据库相关

```bash
# 生成数据库迁移文件
pnpm db:generate
# 基于 src/db/schema.ts 的变更
# 输出: drizzle/ 目录

# 应用迁移到数据库
pnpm db:migrate
# 在生产前必须运行

# 直接同步 Schema 到数据库 (开发用)
pnpm db:push
# ⚠️  仅在开发环境使用，不生成迁移文件

# 打开 Drizzle Studio (数据库管理界面)
pnpm db:studio
# 访问 https://local.drizzle.studio
# 可视化查看和修改数据库数据
```

---

## 📦 代码质量

```bash
# 运行 Biome linter (检查并自动修复代码问题)
pnpm lint
# 自动修复可修复的问题并输出检查结果
# 使用 --write 标志，会自动应用修复

# 格式化代码 (Biome formatter)
pnpm format
# 统一代码风格

# 完整的代码质量检查
pnpm lint && pnpm format
```

---

## 📚 内容和文档

```bash
# 处理 MDX 内容集合
pnpm content
# 生成类型和索引

# 启动邮件模板开发服务器
pnpm email
# 访问 http://localhost:3333
# 预览邮件模板
```

---

## 🧹 清理和维护

```bash
# 清除构建缓存
rm -rf .next
# 清除 Next.js 构建输出

# 重新安装依赖
pnpm install
# 或: pnpm i

# 检查过期依赖
pnpm outdated

# 更新依赖到最新
pnpm update
# ⚠️  可能引入破坏性改动
```

---

## 🔍 快速搜索

### 我想...

| 需求 | 命令 |
|------|------|
| 启动应用 | `pnpm dev` |
| 构建应用 | `pnpm build` |
| 修复代码格式 | `pnpm lint && pnpm format` |
| 查看数据库 | `pnpm db:studio` |
| 创建迁移 | `pnpm db:generate` |
| 应用迁移 | `pnpm db:migrate` |
| 预览邮件 | `pnpm email` |
| 清除缓存 | `rm -rf .next` |

---

## 📋 完整命令列表

```bash
# 开发
pnpm dev               # 启动开发服务器
pnpm build             # 构建生产版本
pnpm start             # 启动生产服务器

# 数据库
pnpm db:generate      # 生成迁移文件
pnpm db:migrate       # 应用迁移
pnpm db:push          # 同步到数据库
pnpm db:studio        # 打开数据库管理界面

# 代码质量
pnpm lint             # 检查并自动修复代码
pnpm format           # 格式化代码

# 内容
pnpm content          # 处理 MDX 内容
pnpm email            # 邮件模板开发

# 包管理
pnpm install          # 安装依赖
pnpm add <pkg>        # 添加依赖
pnpm remove <pkg>     # 删除依赖
pnpm outdated         # 检查过期依赖
```

---

## ⚙️ 环境变量

重要的环境变量 (配置在 `.env.local`):

```bash
# 数据库
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# 认证
BETTER_AUTH_SECRET=your-secret-key

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# 邮件 (Resend)
RESEND_API_KEY=re_...

# AI 服务
OPENAI_API_KEY=sk_...

# AWS S3
AWS_ACCESS_KEY_ID=...
AWS_SECRET_ACCESS_KEY=...

# 验证码 (Turnstile)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=...
```

查看 `env.example` 获取完整列表。

---

## 🐛 常见问题速查

| 问题 | 解决方案 |
|------|---------|
| 构建失败 | `pnpm install` → `pnpm build` |
| 数据库连接失败 | 检查 DATABASE_URL，运行 `pnpm db:migrate` |
| 类型错误 | 运行 `pnpm content` 和 `pnpm lint` |
| 样式不生效 | 检查 TailwindCSS 配置，重启 dev 服务器 |
| 热重载不工作 | 清除 .next，重启 `pnpm dev` |

---

## 💡 开发工作流示例

```bash
# 1. 启动开发
pnpm dev

# 2. 在另一个终端 - 修改代码后检查质量
pnpm lint
pnpm format

# 3. 数据库变更
# 修改 src/db/schema.ts
pnpm db:generate        # 生成迁移
pnpm db:migrate         # 应用迁移
pnpm db:studio          # 验证数据

# 4. 打包前检查
pnpm lint
pnpm build

# 5. 如果构建成功，提交代码
git add .
git commit -m "feat: add new feature"
git push
```

---

## 📖 获取更多帮助

- 查看 `package.json` 中的 `scripts` 部分
- 运行 `pnpm help <command>`
- 查看 [pnpm 官方文档](https://pnpm.io)

