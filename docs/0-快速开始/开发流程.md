# 🔄 开发流程指南

本文档描述在本项目中从需求到上线的完整开发流程。

---

## 📋 完整开发流程

```
需求分析
  ↓
设计方案 (是否需要数据库改动?)
  ↓
选择开发模式 (页面 / API / 组件 / Hook)
  ↓
实施开发
  ↓
本地测试
  ↓
代码审查
  ↓
合并代码
  ↓
上线部署
```

---

## 1️⃣ 需求分析

### 问题清单

在开始开发前，明确以下问题：

```
□ 这是什么功能? (新增 / 修改 / 删除)
□ 影响的用户是谁? (新用户 / 已登录 / 管理员)
□ 涉及哪些页面? (列出所有相关页面)
□ 涉及哪些数据? (需要修改数据库吗?)
□ 涉及哪些权限? (是否需要权限检查?)
□ 涉及外部 API 吗? (Stripe / 邮件 / AI 等)
□ 预计完成时间? (小 / 中 / 大)
```

### 获取帮助

找不到答案？

1. 查看 [FAQ.md](./FAQ.md)
2. 搜索相关的图表文档
3. 询问项目维护者

---

## 2️⃣ 设计方案

### 检查清单

根据需求，检查以下内容：

#### 数据库变更

```
问: 需要修改数据库吗?

是:
  ├─ 添加新表?
  ├─ 修改现有表?
  ├─ 添加新字段?
  └─ 修改字段类型?

处理步骤:
  1. 修改 src/db/schema.ts
  2. 运行 pnpm db:generate
  3. 运行 pnpm db:migrate
  4. 测试数据库连接

否:
  └─ 继续下一步
```

#### 权限和认证

```
问: 需要新的权限检查吗?

是:
  ├─ 需要用户认证?
  ├─ 需要管理员权限?
  └─ 需要特定的套餐权限?

处理步骤:
  1. 确定权限级别 (actionClient / userActionClient / adminActionClient)
  2. 在 Server Action 中添加权限检查
  3. 在页面中添加权限导航

否:
  └─ 继续下一步
```

#### 外部 API 集成

```
问: 需要集成外部 API 吗?

是:
  ├─ Stripe? → 查看 src/payment/
  ├─ 邮件? → 查看 src/mail/
  ├─ AI? → 查看 src/ai/
  ├─ 存储? → 查看 src/storage/
  └─ 其他? → 创建新模块

处理步骤:
  1. 在 src/lib/ 中创建服务模块
  2. 配置 API 密钥 (.env.local)
  3. 实现 API 调用
  4. 添加错误处理
  5. 测试集成

否:
  └─ 继续下一步
```

---

## 3️⃣ 选择开发模式

根据功能类型，选择合适的开发模式。

### 模式 1: 创建新页面

**何时使用**: 添加新的用户界面页面

**步骤**:

```bash
# 1. 确定页面位置和路由
# 位置: src/app/[locale]/(分组)/目录/page.tsx

# 2. 创建页面文件
mkdir -p src/app/[locale]/(marketing)/(pages)/example
touch src/app/[locale]/(marketing)/(pages)/example/page.tsx

# 3. 编写页面内容
# src/app/[locale]/(marketing)/(pages)/example/page.tsx
export default function ExamplePage() {
  return (
    <div>
      <h1>Example Page</h1>
    </div>
  )
}

# 4. 如果需要 layout，创建 layout.tsx
# src/app/[locale]/(marketing)/(pages)/example/layout.tsx

# 5. 本地测试
# http://localhost:3005/en/example
```

**关键点**:
- 遵循现有的页面分组结构
- 考虑国际化路由 ([locale])
- 使用 Radix UI 和 TailwindCSS
- 添加适当的元数据

---

### 模式 2: 创建新组件

**何时使用**: 创建可复用的 UI 组件

**步骤**:

```bash
# 1. 确定组件类型和位置
# 位置: src/components/(分类)/component-name.tsx

# 2. 创建组件文件
touch src/components/custom/my-component.tsx

# 3. 编写组件
import { FC } from 'react'

interface MyComponentProps {
  title: string
  description?: string
  onAction?: () => void
}

export const MyComponent: FC<MyComponentProps> = ({
  title,
  description,
  onAction
}) => {
  return (
    <div className="...">
      <h3>{title}</h3>
      {description && <p>{description}</p>}
      <button onClick={onAction}>Action</button>
    </div>
  )
}

# 4. 在页面中使用
import { MyComponent } from '@/components/custom/my-component'

export default function Page() {
  return <MyComponent title="Hello" onAction={() => {}} />
}

# 5. 测试组件
```

**关键点**:
- 使用 TypeScript 类型
- 提供清晰的 Props 接口
- 使用 Radix UI 组件基础
- 遵循单一职责原则

---

### 模式 3: 创建 Server Action

**何时使用**: 创建服务器端 API 端点

**步骤**:

```bash
# 1. 确定 Action 类型
# actionClient (公开) / userActionClient (需登录) / adminActionClient (管理员)

# 2. 创建 Action 文件
touch src/actions/my-action.ts

# 3. 编写 Action
import { userActionClient } from '@/lib/safe-action'
import { z } from 'zod'

export const myAction = userActionClient
  .schema(z.object({
    id: z.string(),
    name: z.string().min(1)
  }))
  .action(async ({ parsedInput, ctx }) => {
    // ctx 包含: user, userAgent 等
    const { id, name } = parsedInput

    try {
      // 业务逻辑
      const result = await db.update(...)

      return { success: true, data: result }
    } catch (error) {
      return { success: false, error: error.message }
    }
  })

# 4. 在组件中使用
'use client'

import { useAction } from 'next-safe-action/hooks'
import { myAction } from '@/actions/my-action'

export function MyForm() {
  const { execute, isExecuting } = useAction(myAction)

  return (
    <form onSubmit={(e) => {
      e.preventDefault()
      execute({ id: '123', name: 'New Name' })
    }}>
      <button disabled={isExecuting}>Submit</button>
    </form>
  )
}

# 5. 测试 Action
```

**关键点**:
- 使用 Zod 验证输入
- 处理所有可能的错误
- 返回明确的成功/失败状态
- 检查权限

---

### 模式 4: 创建自定义 Hook

**何时使用**: 共享业务逻辑或状态管理

**步骤**:

```bash
# 1. 确定 Hook 用途
# 数据获取 / 状态管理 / 事件处理 / 计算

# 2. 创建 Hook 文件
touch src/hooks/use-my-data.ts

# 3. 编写 Hook (数据获取示例)
import { useQuery } from '@tanstack/react-query'
import { fetchMyData } from '@/actions/fetch-my-data'

export const useMyData = (id: string) => {
  return useQuery({
    queryKey: ['myData', id],
    queryFn: () => fetchMyData(id),
    enabled: !!id
  })
}

# 4. 在组件中使用
'use client'

import { useMyData } from '@/hooks/use-my-data'

export function MyComponent({ id }: { id: string }) {
  const { data, isLoading, error } = useMyData(id)

  if (isLoading) return <div>Loading...</div>
  if (error) return <div>Error: {error.message}</div>

  return <div>{data?.title}</div>
}

# 5. 测试 Hook
```

**关键点**:
- 使用 React Query 处理异步数据
- 提供清晰的返回值
- 处理加载和错误状态
- 遵循 Hook 命名约定 (use*)

---

### 模式 5: 创建 API Route

**何时使用**: 创建 HTTP API 端点 (较少使用，通常用 Server Actions)

**步骤**:

```bash
# 1. 创建 API 文件
touch src/app/api/my-endpoint/route.ts

# 2. 编写 API
import { NextRequest, NextResponse } from 'next/server'
import { auth } from '@/lib/auth'

export async function POST(request: NextRequest) {
  try {
    // 验证认证 (可选)
    const session = await auth.api.getSession({
      headers: request.headers
    })

    if (!session?.user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    // 解析请求
    const body = await request.json()

    // 业务逻辑
    const result = await processData(body)

    return NextResponse.json({ success: true, data: result })
  } catch (error) {
    return NextResponse.json(
      { error: error.message },
      { status: 500 }
    )
  }
}
```

**关键点**:
- 处理认证和错误
- 验证输入
- 返回适当的 HTTP 状态码

---

## 4️⃣ 实施开发

### 编码规范

遵循项目的编码规范：

```typescript
// 使用 TypeScript
const myFunction = (param: string): Promise<boolean> => {
  // 业务逻辑
}

// 使用单引号
const message = 'Hello World'

// 使用尾部逗号
const obj = {
  key1: 'value1',
  key2: 'value2',
}

// 添加注释
// 这是一个重要的函数
export const importantFunction = () => {}

// 使用 Zod 验证
const schema = z.object({
  email: z.string().email(),
  age: z.number().min(18)
})
```

### Git 工作流

```bash
# 1. 从主分支拉取最新代码
git checkout main
git pull origin main

# 2. 创建功能分支
git checkout -b feat/add-new-feature

# 3. 开发和提交
# ... 开发代码 ...
git add .
git commit -m "feat: add new feature"

# 4. 推送到远程
git push origin feat/add-new-feature

# 5. 创建 PR
# 在 GitHub 上创建 Pull Request
```

### 提交信息规范

使用清晰的提交信息：

```
feat: 添加新功能
fix: 修复 bug
docs: 更新文档
style: 格式化代码
refactor: 重构代码
test: 添加测试
chore: 更新依赖
```

---

## 5️⃣ 本地测试

### 测试清单

```
□ 功能正常工作
□ 没有 TypeScript 错误
□ 没有 console 警告
□ 响应式设计正常
□ 权限检查有效
□ 错误处理正确
□ 暗/亮模式正常
□ 国际化翻译正确 (如果涉及)
□ 数据持久化正确 (数据库)
□ 第三方集成正常 (API)
```

### 常见测试场景

```bash
# 1. 清空浏览器缓存
localStorage.clear()
sessionStorage.clear()

# 2. 测试不同屏幕大小
# Desktop: 1920x1080
# Tablet: 768x1024
# Mobile: 375x812

# 3. 测试错误边界
# 网络错误 (关闭网络)
# 权限错误 (修改用户角色)
# 验证错误 (提交无效数据)

# 4. 测试性能
# 打开 DevTools → Performance
# 检查加载时间和渲染性能
```

---

## 6️⃣ 代码审查

在提交 PR 前，自我检查：

### PR 检查清单

```
代码质量:
□ 代码遵循项目规范
□ 使用 TypeScript 类型
□ 有适当的错误处理
□ 没有重复代码

功能:
□ 功能完整实现
□ 所有边界情况处理
□ 用户输入验证
□ 权限检查

文档:
□ 添加了代码注释
□ 更新了相关文档
□ 提供了 PR 描述
□ 描述了测试方式

性能:
□ 没有明显的性能问题
□ 数据库查询优化
□ React 渲染优化
□ 包大小合理
```

### PR 描述模板

```markdown
## 描述
简要描述这个 PR 做了什么

## 类型
- [ ] 新功能
- [ ] Bug 修复
- [ ] 文档更新
- [ ] 重构

## 相关 Issue
Closes #123

## 测试方式
说明如何测试这个功能

## 截图 (如适用)
添加相关的截图

## 检查清单
- [ ] 代码遵循项目规范
- [ ] 添加了适当的注释
- [ ] 更新了文档
- [ ] 本地测试通过
```

---

## 7️⃣ 合并代码

### 代码审查反馈

如果有审查意见：

```
1. 阅读反馈
2. 讨论不同意见
3. 进行必要的更改
4. 推送更新
5. 请求再次审查
```

### 合并代码

```bash
# 确保代码已获批准
# 点击 GitHub 上的 "Merge" 按钮

# 或本地合并:
git checkout main
git pull origin main
git merge feat/add-new-feature
git push origin main
```

---

## 8️⃣ 上线部署

### 部署检查

```
□ 代码已合并到 main
□ 所有 CI/CD 检查通过
□ 数据库迁移已完成
□ 环境变量已配置
□ 构建成功 (pnpm build)
□ 功能已测试
```

### 部署步骤

```bash
# 如果使用 Vercel:
# 推送到 main 后会自动部署

# 或手动部署:
# 1. 构建项目
pnpm build

# 2. 启动生产服务器
pnpm start

# 3. 验证部署
# 检查生产环境中的功能
```

---

## 🔁 完整流程示例

### 例子：添加用户头像功能

```
1. 需求分析
   □ 添加用户头像上传功能
   □ 影响用户: 所有已登录用户
   □ 涉及页面: /settings/profile
   □ 涉及数据: user.image 字段
   □ 涉及外部 API: AWS S3 (存储)

2. 设计方案
   □ 数据库: user 表已有 image 字段，无需改动
   □ 权限: userActionClient (需要登录)
   □ 外部 API: S3 存储

3. 选择模式
   □ 创建 UpdateAvatarCard 组件
   □ 创建 updateAvatarAction Server Action
   □ 使用 S3 上传文件

4. 实施开发
   ├─ 创建上传组件 (src/components/settings/profile/update-avatar-card.tsx)
   ├─ 创建 Action (src/actions/update-avatar.ts)
   ├─ 集成 S3 上传 (src/lib/s3-upload.ts)
   └─ 添加 UI 交互

5. 本地测试
   ├─ 上传头像
   ├─ 验证显示
   ├─ 测试错误处理
   └─ 检查响应式

6. 代码审查
   ├─ 提交 PR
   ├─ 等待审查
   └─ 进行修改

7. 合并代码
   ├─ 获得批准
   └─ 合并到 main

8. 上线部署
   ├─ Vercel 自动部署
   └─ 验证生产环境
```

---

## 💡 提示和最佳实践

### 开发技巧

1. **保持提交小而集中** - 一个 PR 一个功能
2. **频繁提交** - 便于回滚和追踪
3. **写有意义的 commit 消息** - 帮助其他开发者理解
4. **测试边界情况** - 不仅测试成功路径
5. **考虑性能** - 优化查询和渲染

### 常见陷阱

```
❌ 修改了不相关的代码
✅ 只修改相关的代码

❌ 没有处理错误
✅ 添加 try-catch 和错误提示

❌ 忽视权限检查
✅ 始终验证用户权限

❌ 没有考虑国际化
✅ 使用 useTranslations() Hook

❌ 修改了数据库但没有迁移
✅ 运行 pnpm db:generate 和 db:migrate
```

---

## 📞 需要帮助？

- 查看 [FAQ.md](./FAQ.md)
- 搜索相关的图表文档
- 询问项目维护者

祝你开发顺利！🚀

