# 博文替换验证方案

> 目标：用 200 篇新 PPT 博文替换 MkSaaS 模板的示例博文

## 一、当前状态

### 现有博文
```
content/blog/
├── algorithm.mdx / algorithm.zh.mdx        # 模板示例
├── creem.mdx / creem.zh.mdx                # 模板示例
├── directory.mdx / directory.zh.mdx        # 模板示例
├── dokploy.mdx / dokploy.zh.mdx            # 模板示例
├── email.mdx / email.zh.mdx                # 模板示例
├── haitang.mdx / haitang.zh.mdx            # 模板示例
├── obsidian.mdx / obsidian.zh.mdx          # 模板示例
├── premium.mdx / premium.zh.mdx            # 模板示例
├── shijing.mdx / shijing.zh.mdx            # 模板示例
├── ppt-category-complete-guide.mdx/.zh.mdx # 保留（PPT 分类指南）
└── ppt/                                     # 新 PPT 博文（400 个文件）
```

### 新博文
- **位置**: `content/blog/ppt/`
- **数量**: 200 篇（400 个文件，含中英双语）
- **分类**: 8 个子目录（商务汇报、年终总结、述职报告等）

## 二、替换策略

### 方案 A：完全替换（推荐）✅

**操作**:
1. 备份模板示例博文到 `content/blog/_template-examples/`
2. 删除根目录的 9 篇模板示例（保留 `ppt-category-complete-guide`）
3. 保留 `content/blog/ppt/` 新博文
4. 测试验证

**优点**:
- 干净清爽，只保留真实内容
- 避免混淆
- 便于后续维护

**缺点**:
- 失去模板参考（已备份可恢复）

### 方案 B：共存模式

**操作**:
1. 保留模板示例博文
2. 在首页/博客列表中过滤掉模板示例
3. 通过 frontmatter 添加 `template: true` 标记

**优点**:
- 保留模板参考
- 可随时切换

**缺点**:
- 需要修改代码过滤逻辑
- 可能造成混淆

## 三、执行步骤（方案 A）

### 步骤 1: 备份模板示例
```bash
mkdir -p content/blog/_template-examples
mv content/blog/algorithm.* content/blog/_template-examples/
mv content/blog/creem.* content/blog/_template-examples/
mv content/blog/directory.* content/blog/_template-examples/
mv content/blog/dokploy.* content/blog/_template-examples/
mv content/blog/email.* content/blog/_template-examples/
mv content/blog/haitang.* content/blog/_template-examples/
mv content/blog/obsidian.* content/blog/_template-examples/
mv content/blog/premium.* content/blog/_template-examples/
mv content/blog/shijing.* content/blog/_template-examples/
```

### 步骤 2: 验证新博文
```bash
# 检查博文数量
find content/blog/ppt -name "*.mdx" | wc -l

# 检查分类分布
for dir in content/blog/ppt/*/; do 
  echo "$(basename "$dir"): $(find "$dir" -name "*.mdx" | wc -l) 个"
done

# 检查图片链接
grep -r "image:" content/blog/ppt/ | head -5
```

### 步骤 3: 启动开发服务器测试
```bash
pnpm dev
```

**测试清单**:
- [ ] 首页博客列表显示正常
- [ ] 博客详情页打开正常
- [ ] 图片加载正常
- [ ] 中英文切换正常
- [ ] 分类筛选正常
- [ ] 搜索功能正常
- [ ] 相关文章推荐正常
- [ ] SEO 元数据正常

### 步骤 4: 检查路由配置

**检查文件**: `src/app/[locale]/blog/[...slug]/page.tsx`

确认路由支持：
- 根目录博文: `/blog/article-name`
- 子目录博文: `/blog/ppt/category/article-name`

### 步骤 5: 更新导航和配置

**可能需要更新的文件**:
- `src/config/website.tsx` - 博客分类配置
- `src/config/nav-config.tsx` - 导航菜单
- `src/app/[locale]/blog/page.tsx` - 博客列表页

## 四、验证检查点

### 4.1 前端显示
- [ ] 博客列表页显示 200 篇新博文
- [ ] 分类筛选显示 8 个分类
- [ ] 博文卡片显示封面图、标题、描述
- [ ] 点击进入详情页正常

### 4.2 博文详情
- [ ] 封面图显示正常
- [ ] 标题、描述、日期显示正常
- [ ] 正文内容渲染正常
- [ ] 内部链接可点击
- [ ] 外部链接可打开
- [ ] FAQ 段落显示正常
- [ ] 权威引用显示正常

### 4.3 SEO 检查
- [ ] `<title>` 标签正确
- [ ] `<meta description>` 正确
- [ ] Open Graph 标签正确
- [ ] 图片 alt 属性存在
- [ ] 结构化数据正确

### 4.4 性能检查
- [ ] 首页加载速度 < 3s
- [ ] 博文详情页加载速度 < 2s
- [ ] 图片懒加载生效
- [ ] 图片压缩生效（< 500KB）

### 4.5 多语言检查
- [ ] 中文博文路径: `/zh/blog/ppt/...`
- [ ] 英文博文路径: `/blog/ppt/...`
- [ ] 语言切换按钮正常
- [ ] 切换后内容对应

## 五、回滚方案

如果测试发现问题，可以快速回滚：

```bash
# 恢复模板示例
mv content/blog/_template-examples/* content/blog/

# 或恢复旧 PPT 博文
rm -rf content/blog/ppt
mv content/blog/ppt.backup content/blog/ppt
```

## 六、执行脚本

创建自动化脚本 `scripts/replace-blog.sh`:

```bash
#!/bin/bash
set -e

echo "🔄 开始替换博文..."

# 1. 备份模板示例
echo "📦 备份模板示例..."
mkdir -p content/blog/_template-examples
mv content/blog/algorithm.* content/blog/_template-examples/ 2>/dev/null || true
mv content/blog/creem.* content/blog/_template-examples/ 2>/dev/null || true
mv content/blog/directory.* content/blog/_template-examples/ 2>/dev/null || true
mv content/blog/dokploy.* content/blog/_template-examples/ 2>/dev/null || true
mv content/blog/email.* content/blog/_template-examples/ 2>/dev/null || true
mv content/blog/haitang.* content/blog/_template-examples/ 2>/dev/null || true
mv content/blog/obsidian.* content/blog/_template-examples/ 2>/dev/null || true
mv content/blog/premium.* content/blog/_template-examples/ 2>/dev/null || true
mv content/blog/shijing.* content/blog/_template-examples/ 2>/dev/null || true

echo "✅ 备份完成"

# 2. 验证新博文
echo "🔍 验证新博文..."
blog_count=$(find content/blog/ppt -name "*.mdx" | wc -l | xargs)
echo "博文数量: $blog_count"

if [ "$blog_count" -ne 400 ]; then
  echo "❌ 博文数量不正确，预期 400，实际 $blog_count"
  exit 1
fi

# 3. 检查图片
echo "🖼️  检查图片..."
image_count=$(find public/images/blog/ppt -type f | wc -l | xargs)
echo "图片数量: $image_count"

echo ""
echo "✅ 替换完成！"
echo ""
echo "📋 下一步："
echo "1. 运行 pnpm dev 启动开发服务器"
echo "2. 访问 http://localhost:3005/blog 检查博客列表"
echo "3. 测试几篇博文详情页"
echo "4. 检查中英文切换"
echo ""
echo "如需回滚，运行: mv content/blog/_template-examples/* content/blog/"
```

## 七、后续优化

替换完成后，可以考虑：

1. **更新首页**: 展示最新 PPT 博文
2. **添加分类页**: 为 8 个分类创建专属页面
3. **优化搜索**: 确保新博文可被搜索
4. **添加标签**: 为博文添加标签系统
5. **RSS 订阅**: 更新 RSS feed
6. **Sitemap**: 重新生成 sitemap

## 八、注意事项

⚠️ **重要提醒**:
1. 替换前务必备份
2. 在开发环境充分测试
3. 检查所有图片链接
4. 验证 SEO 元数据
5. 测试多语言切换
6. 确认构建无错误（`pnpm build`）

## 九、预期效果

替换后：
- ✅ 博客列表显示 200 篇真实 PPT 博文
- ✅ 8 个专业分类清晰展示
- ✅ 中英双语完整支持
- ✅ SEO 优化到位
- ✅ 图片加载快速
- ✅ 用户体验流畅
