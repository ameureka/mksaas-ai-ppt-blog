## 基于v0 开发的ui    位置在：/Users/ameureka/Desktop/v0mksaaspptsite

## 需要集成到mk-saas-blog  位置在：/Users/ameureka/Desktop/mksaas-blog


## 任务介绍
  - 页面层（营销）
        - app/page.tsx, app/categories/page.tsx, app/category/[name]/page.tsx：纯前台内容页，无 admin 路由。落位到 src/app/[locale]/(marketing)/...。需要：移除 MksaasPublicLayout，接入真实 (marketing)/layout.tsx，导入改为 @/，文案做 i18n。复杂
          度：中。
        - 无 admin 页面文件，需在目标仓新建 src/app/[locale]/(protected)/admin/... 对应路由，内容来源于组件+client 组合（见下）。
    - Admin 组件 / 客户端胶水（需落位 src/components/admin/{feature} + 新建 page client）
        - 列表/表单/图表类：components/admin/ppt-list-table.tsx、ppt-edit-form.tsx、ppt-delete-dialog.tsx、user-list-table.tsx、category-distribution-chart.tsx、download-trend-chart.tsx、top-ppt-list.tsx、stats-card.tsx。
            - 复杂度：中。需：修导入、i18n、hook 接线（替换 mock），按功能拆目录，表格保持纯 UI。
        - 需要补 Page Client（nuqs 状态 + Hook 调用）和 Admin 页面：当前缺失；可用模板/参考 mksaas-blog 的 users-page-client 模式。复杂度：中高。
    - 通用/公共组件
        - navigation-header.tsx, ppt-card.tsx, search-filters.tsx, download/auth 模块 (download-flow/download-modal.tsx, download/download-options-modal.tsx, auth/login-modal.tsx)，广告组件 components/ads/*，search-sidebar.tsx。
            - 复杂度：中。需：路径落位（shared/marketing）、i18n、导入修复，避免覆盖 UI 基础。广告/下载/Auth 是否接入现有体系需确认业务。
    - Mock 布局（需删除替换）
        - components/mksaas-preview-layout.tsx, mksaas-dashboard-header.tsx, mksaas-public-layout.tsx：集成时全部移除，改用 (protected)/layout.tsx + DashboardHeader / (marketing)/layout.tsx。复杂度：低（清理性工作）。
    - UI 基础
        - components/ui/*：与目标仓已有 shadcn 重复，默认不迁移，保留目标仓版本。复杂度：低（忽略/对齐即可）。
    - Hooks
        - 数据/变更 Hooks：hooks/use-get-ppts.ts, use-get-ppt.ts, use-create-ppt.ts, use-update-ppt.ts, use-delete-ppt.ts, use-get-users.ts, use-get-user.ts, use-update-user.ts, use-ban-user.ts, use-adjust-credits.ts, use-get-dashboard-
  stats.ts,
          use-update-settings.ts。
            - 复杂度：中。需：落位 src/hooks/，改为调用真实 Safe Actions（现是 mock），校准 queryKey，与 mksaas-blog 的 use-users 模式对齐。
        - 设备/杂项：use-mobile.ts, use-rewarded-video.ts, use-toast.ts（重复目标仓已有 use-toast），视需求取用或忽略。复杂度：低。
    - Types / Schemas / Constants
        - lib/types/*, lib/schemas/*, lib/constants/routes.ts, lib/constants/i18n.ts：可迁到 src/types/、src/lib/validations/、src/lib/constants/。需对齐目标仓已有类型（如 auth-types）。复杂度：中。
        - lib/admin/permissions.tsx, admin/audit.ts：需看目标仓权限模型后决定是否保留/合并。复杂度：中。
        - lib/mock-data/*, lib/actions-mock.ts：仅供 v0 预览，集成时删除或停用。复杂度：低。
    - API/服务（多为 mock）
        - lib/api/mock/*, lib/api/services/*, lib/api/client.ts, index.ts：Mock/占位，集成时不用，改用真实 Safe Actions。复杂度：低（清理）。
    - Styles
        - app/globals.css：如需引入变量/样式，需与目标仓 src/styles/全局样式合并，谨慎处理。复杂度：中高（合并风险）。
    - 优先级建议（先易后难，按价值）
        1. Admin 组件 + 相应 Hooks/Types/Schemas：先完成核心后台（PPT/用户/统计），创建对应 page.tsx + client 组件。
        2. 清理 Mock 布局（全局一次性）+ 导入修复。
        3. 营销页迁移（page.tsx/categories/category）接入真实 layout + i18n。
        4. 其余公共组件/广告/下载/Auth 按业务需要择优迁移。
        5. 样式合并如无强需可延后或按需摘取变量。

    如果你指定先迁哪个功能（如 PPT 管理或用户管理），我可以按上述优先级开始实际迁移与接线。这个任务的我们，主线任务我们执行到什么的地方了


## 个人经验，还是要使用claude code 做大型的项目的设计的规划的，
codex 整体上看确实还是不行，综合的规划的有很多的错误的信息，容易产生严重的误导信息，还要返工的处理的，这个很烦。

### 实际上一个产品的开发的逻辑的还是很复杂的，根本就不是在前端的，这个是必须的。
