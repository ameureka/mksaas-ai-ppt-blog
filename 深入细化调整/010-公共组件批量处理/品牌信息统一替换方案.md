# Content 目录品牌信息统一替换方案

## 📝 现状分析

### 1. 作者信息 (`content/author/`)

当前存在 **5 个作者身份**，每个都有中英文版本：

| 作者 ID | 名称 | 头像路径 | 用途 |
|---------|------|----------|------|
| `fox` | Fox | `/images/avatars/fox.png` | 主要博客作者 (最常用) |
| `mksaas` | MkSaaS | `/images/avatars/mksaas.png` | 品牌官方作者 |
| `haitang` | Haitang | `/images/avatars/haitang.png` | 特定内容作者 |
| `mkdirs` | Mkdirs | `/images/avatars/mkdirs.png` | 辅助作者 |
| `pptx-team` | PPTX Team | `/images/authors/pptx-team.jpg` | PPT 团队作者 |

**统计数据**：
- 绝大多数博客文章使用 `author: fox`
- PPT 相关文章主要使用 `fox`，少量使用 `pptx-team`
- 总计约 **204 篇博客文章** 需要处理

### 2. 品牌名称出现位置

在以下位置可能出现品牌名称：
- **作者信息**: `author: fox/mksaas/haitang/mkdirs`
- **文章标题**: 部分文章标题可能包含品牌名
- **文章内容**: 正文中可能提及 "MkSaaS", "Indie Maker Fox" 等
- **图片文件名**: 部分图片可能包含品牌水印或名称
- **llms.txt**: 网站索引文件中的品牌描述

### 3. 图片资源统计

- **作者头像**: 5 个头像文件需要替换
  - `/images/avatars/*.png` (4 个)
  - `/images/authors/pptx-team.jpg` (1 个)

- **博客封面图**: 约 78+ 个图片文件
  - 部分可能包含品牌水印
  - 路径: `/images/blog/**/*.{jpg,png}`

---

## 🎯 替换策略建议

### 方案 A：保守替换（推荐）

**目标**: 最小化改动，保持内容结构不变

#### 步骤 1: 准备新品牌资源
```bash
# 1. 准备新的作者头像 (推荐 512x512px)
/public/images/avatars/
  ├── new-brand-avatar.png  # 新品牌主头像
  └── team-avatar.png       # 团队头像（如需要）

# 2. 准备新的品牌 Logo
/public/
  ├── logo.png
  └── logo-dark.png
```

#### 步骤 2: 更新作者配置
```bash
# 方案 A1: 保留原有作者 ID，仅修改显示名称和头像
# 修改 content/author/*.mdx

# fox.mdx → 改为新品牌主作者
---
name: Your New Brand Name
avatar: /images/avatars/new-brand-avatar.png
---

# pptx-team.mdx → 可保留或合并
```

#### 步骤 3: 批量替换文本内容
```bash
# 使用脚本批量替换品牌名称
# 创建 scripts/replace-brand-names.ts

替换规则:
- "MkSaaS" → "YourBrand"
- "Indie Maker Fox" → "Your Brand Tagline"
- "mksaas.me" → "yourdomain.com"
```

#### 步骤 4: 更新图片资源
```bash
# 选项 1: 覆盖原有头像文件（快速）
# 直接替换 /public/images/avatars/ 下的文件

# 选项 2: 更新路径引用（彻底）
# 批量修改 MDX 文件中的 avatar 字段
```

---

### 方案 B：彻底重构

**目标**: 建立统一的品牌标识体系

#### 步骤 1: 统一作者身份
```bash
# 合并所有作者为一个主品牌身份
content/author/
  ├── official.mdx      # 官方账号（英文）
  └── official.zh.mdx   # 官方账号（中文）

# 删除其他作者文件
```

#### 步骤 2: 批量更新 author 字段
```typescript
// scripts/unify-authors.ts
- author: fox
- author: mksaas
- author: haitang
+ author: official
```

#### 步骤 3: 清理图片水印
```bash
# 检查所有博客封面图是否包含旧品牌水印
# 使用 ImageMagick 或 AI 工具批量去除水印
```

---

## 🛠️ 实施工具与脚本

### 1. 批量替换脚本框架

```typescript
// scripts/replace-brand-info.ts
import { readdirSync, readFileSync, writeFileSync } from 'fs';
import { join } from 'path';

const REPLACEMENTS = {
  // 作者字段替换
  authors: {
    'author: fox': 'author: official',
    'author: mksaas': 'author: official',
    'author: haitang': 'author: official',
  },

  // 文本内容替换
  text: {
    'MkSaaS': 'YourBrand',
    'Indie Maker Fox': 'Your Tagline',
    'mksaas.me': 'yourdomain.com',
  },

  // 图片路径替换
  images: {
    '/images/avatars/fox.png': '/images/avatars/official.png',
    '/images/avatars/mksaas.png': '/images/avatars/official.png',
  }
};

function processDirectory(dir: string) {
  const files = readdirSync(dir, { withFileTypes: true });

  for (const file of files) {
    const fullPath = join(dir, file.name);

    if (file.isDirectory()) {
      processDirectory(fullPath);
    } else if (file.name.endsWith('.mdx')) {
      let content = readFileSync(fullPath, 'utf-8');

      // 执行替换
      for (const [old, newVal] of Object.entries(REPLACEMENTS.authors)) {
        content = content.replace(new RegExp(old, 'g'), newVal);
      }

      for (const [old, newVal] of Object.entries(REPLACEMENTS.text)) {
        content = content.replace(new RegExp(old, 'g'), newVal);
      }

      writeFileSync(fullPath, content);
      console.log(`✅ Updated: ${fullPath}`);
    }
  }
}

processDirectory('./content');
```

### 2. 检查脚本

```typescript
// scripts/check-brand-references.ts
// 扫描所有品牌引用，生成报告

检查项:
- [ ] 所有 author 字段的使用情况
- [ ] 文章内容中的品牌名称
- [ ] 图片文件中的品牌水印
- [ ] llms.txt 中的品牌描述
- [ ] 配置文件中的品牌信息
```

---

## ✅ 检查清单

### 文件级别
- [ ] `content/author/*.mdx` - 作者信息更新
- [ ] `content/blog/**/*.mdx` - 博客文章 frontmatter
- [ ] `public/llms.txt` - AI 索引文件
- [ ] `public/logo*.png` - 品牌 Logo
- [ ] `public/images/avatars/*.png` - 头像图片
- [ ] `public/images/authors/*.jpg` - 作者照片

### 配置级别
- [ ] `src/config/website.tsx` - 网站配置
- [ ] `messages/en.json` & `messages/zh.json` - 国际化文本
- [ ] `README.md` - 项目说明
- [ ] `package.json` - 项目名称

### 验证步骤
1. 运行 `pnpm dev` 检查是否有构建错误
2. 检查博客页面作者显示是否正确
3. 检查图片是否正常加载
4. 搜索残留的旧品牌名称

---

## 🚨 注意事项

1. **备份**: 执行任何批量替换前，先提交 Git 或创建备份
   ```bash
   git add .
   git commit -m "backup before brand replacement"
   ```

2. **分阶段执行**: 不要一次性替换所有内容
   - 第一阶段：作者信息 + 头像
   - 第二阶段：文章内容
   - 第三阶段：图片资源

3. **保留历史**: 如果需要保留作者多样性（如团队博客），可保留多个作者身份

4. **SEO 考虑**: 品牌名称改变可能影响搜索引擎收录，建议设置 301 重定向

---

## 📊 影响范围评估

| 类型 | 文件数 | 影响程度 |
|------|--------|----------|
| 作者配置 | 10 | 🔴 高 |
| 博客文章 | ~204 | 🔴 高 |
| 头像图片 | 5 | 🟡 中 |
| 博客封面 | 78+ | 🟡 中 |
| 配置文件 | ~5 | 🟢 低 |

**预计工作量**: 2-4 小时（使用自动化脚本）
