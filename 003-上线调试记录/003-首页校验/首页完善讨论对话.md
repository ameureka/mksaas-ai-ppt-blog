# 首页完善讨论对话

**创建时间**: 2025-12-07 01:21
**状态**: 🔄 讨论中

---

## 📋 问题全景图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           首页完善问题全景                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  🔴 P0 - 代码层面 (立即修复)                                                │
│  ├── 1. toPPTDto 未映射 description                                        │
│  ├── 2. toPPTDto 未映射 file_size                                          │
│  ├── 3. PPT 接口 file_size 类型错误 (string → number)                      │
│  ├── 4. 查询未过滤 deleted_at                                              │
│  └── 5. 下载按钮未跳转详情页                                                │
│                                                                             │
│  🟡 P1 - 技术债务 (短期清理)                                                │
│  ├── 6. 重复文件: src/types/ppt.ts                                         │
│  ├── 7. 重复文件: src/schemas/ppt.ts                                       │
│  ├── 8. query-keys 引用旧路径                                              │
│  └── 9. Zod 枚举不同步 (8类 vs 12类)                                       │
│                                                                             │
│  🟠 P1.5 - 数据/业务层面                                                    │
│  ├── 10. 编辑精选/本周新品用同一查询                                        │
│  ├── 11. 首页 12 类 vs DB 9 类                                             │
│  └── 12. 分类图片缺失 (5个分类)                                            │
│                                                                             │
│  🟢 P2 - 体验优化 (后续)                                                    │
│  ├── 13. 分类数量硬编码                                                     │
│  ├── 14. 搜索仅简单 ilike                                                  │
│  └── 15. 卡片可增加 author/file_size 显示                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔴 P0 - 代码层面问题 (立即修复)

### 问题 1: toPPTDto 未映射 description

| 维度 | 说明 |
|------|------|
| **现状** | `description: row.title` - 用 title 回填 |
| **应该** | `description: row.description ?? row.title ?? ''` |
| **影响** | 详情页/SEO 无法使用真实描述 |
| **紧急度** | 🔴 高 - 影响 SEO |
| **工作量** | 5 分钟 |
| **侵入性** | 低 - 仅改 1 行代码 |
| **文件** | `src/actions/ppt/ppt.ts` |

**修复代码**:
```typescript
// 当前
description: row.title ?? '',

// 修复后
description: row.description ?? row.title ?? '',
```

**我的建议**: ✅ 立即修复

---

### 问题 2: toPPTDto 未映射 file_size

| 维度 | 说明 |
|------|------|
| **现状** | `file_size: '未知'` - 硬编码字符串 |
| **应该** | `file_size: row.fileSize ?? 0` |
| **影响** | 详情页无法显示真实文件大小 |
| **紧急度** | 🔴 高 - 数据不准确 |
| **工作量** | 5 分钟 |
| **侵入性** | 低 - 仅改 1 行代码 |
| **文件** | `src/actions/ppt/ppt.ts` |

**修复代码**:
```typescript
// 当前
file_size: '未知',

// 修复后
file_size: row.fileSize ?? 0,
```

**我的建议**: ✅ 立即修复

---

### 问题 3: PPT 接口 file_size 类型错误

| 维度 | 说明 |
|------|------|
| **现状** | `file_size: string` |
| **应该** | `file_size: number` (字节) |
| **影响** | 类型不安全，前端需要额外处理 |
| **紧急度** | 🔴 高 - 与问题 2 配套 |
| **工作量** | 5 分钟 |
| **侵入性** | 中 - 需检查前端使用处 |
| **文件** | `src/lib/types/ppt/ppt.ts` |

**修复代码**:
```typescript
// 当前
file_size: string;

// 修复后
file_size: number;
```

**前端格式化**:
```typescript
const formatFileSize = (bytes: number): string => {
  if (bytes === 0) return '未知';
  const mb = bytes / (1024 * 1024);
  return mb >= 1 ? `${mb.toFixed(1)} MB` : `${(bytes / 1024).toFixed(0)} KB`;
};
```

**我的建议**: ✅ 立即修复（与问题 2 一起）

---

### 问题 4: 查询未过滤 deleted_at

| 维度 | 说明 |
|------|------|
| **现状** | buildWhere 没有过滤软删除记录 |
| **应该** | 添加 `isNull(pptTable.deletedAt)` 条件 |
| **影响** | 软删除的 PPT 可能被查出 |
| **紧急度** | 🔴 高 - 数据正确性 |
| **工作量** | 5 分钟 |
| **侵入性** | 低 - 仅加 1 行条件 |
| **文件** | `src/actions/ppt/ppt.ts` |

**修复代码**:
```typescript
import { isNull } from 'drizzle-orm';

const buildWhere = (params?: PPTListParams) => {
  const conditions = [];
  
  // ✅ 过滤软删除记录
  conditions.push(isNull(pptTable.deletedAt));
  
  // ... 其他条件
};
```

**我的建议**: ✅ 立即修复

---

### 问题 5: 下载按钮未跳转详情页

| 维度 | 说明 |
|------|------|
| **现状** | 点击"立即下载"只显示 toast，不跳转 |
| **应该** | 跳转到 `/ppt/[id]` 详情页 |
| **影响** | 用户体验差，无法真正下载 |
| **紧急度** | 🔴 高 - 核心功能 |
| **工作量** | 5 分钟 |
| **侵入性** | 低 - 改 handleDownload 函数 |
| **文件** | `src/app/[locale]/(marketing)/ppt/page.tsx` |

**修复代码**:
```typescript
// 当前
const handleDownload = (ppt: PPT) => {
  toast.success(`正在准备下载《${ppt.title}》...`);
};

// 修复后
const handleDownload = (ppt: PPT) => {
  router.push(`/ppt/${ppt.id}`);
};
```

**我的建议**: ✅ 立即修复

---

## 🟡 P1 - 技术债务 (短期清理)

### 问题 6: 重复文件 src/types/ppt.ts

| 维度 | 说明 |
|------|------|
| **现状** | 与 `src/lib/types/ppt/ppt.ts` 重复 |
| **引用** | 仅 `src/lib/query-keys.ts` 引用 |
| **影响** | 代码混乱，维护困难 |
| **紧急度** | 🟡 中 |
| **工作量** | 5 分钟 |
| **侵入性** | 低 - 迁移引用后删除 |

**执行步骤**:
1. 修改 `src/lib/query-keys.ts` 的 import
2. 删除 `src/types/ppt.ts`

**我的建议**: ✅ P0 完成后立即清理

---

### 问题 7: 重复文件 src/schemas/ppt.ts

| 维度 | 说明 |
|------|------|
| **现状** | 与 `src/lib/ppt/schemas/ppt.ts` 重复 |
| **引用** | 无引用 |
| **影响** | 代码混乱 |
| **紧急度** | 🟡 中 |
| **工作量** | 1 分钟 |
| **侵入性** | 无 - 直接删除 |

**我的建议**: ✅ 直接删除

---

### 问题 8: query-keys 引用旧路径

| 维度 | 说明 |
|------|------|
| **现状** | `import type { PPTListParams } from '@/types/ppt'` |
| **应该** | `import type { PPTListParams } from '@/lib/types/ppt/ppt'` |
| **影响** | 依赖重复文件 |
| **紧急度** | 🟡 中 |
| **工作量** | 2 分钟 |
| **侵入性** | 低 |
| **文件** | `src/lib/query-keys.ts` |

**我的建议**: ✅ 与问题 6 一起处理

---

### 问题 9: Zod 枚举不同步

| 维度 | 说明 |
|------|------|
| **现状** | `src/lib/ppt/schemas/ppt.ts` 有 8 类 + other |
| **应该** | 同步为 12 类，无 other |
| **引用** | 仅测试文件引用 |
| **影响** | 类型校验不一致 |
| **紧急度** | 🟡 中 |
| **工作量** | 5 分钟 |
| **侵入性** | 低 |

**选项**:
- A: 同步为 12 类
- B: 直接删除这个文件（因为只有测试引用）

**我的建议**: 🤔 需要你决策 - 如果测试不重要，建议删除

---

## 🟠 P1.5 - 数据/业务层面

### 问题 10: 编辑精选/本周新品用同一查询

| 维度 | 说明 |
|------|------|
| **现状** | 两个板块用同一个 API 结果 slice |
| **应该** | 分开查询，不同排序逻辑 |
| **影响** | 内容重复，不符合业务语义 |
| **紧急度** | 🟠 中高 |
| **工作量** | 15 分钟 |
| **侵入性** | 低 |
| **文件** | `src/app/[locale]/(marketing)/ppt/page.tsx` |

**修复代码**:
```typescript
// 当前
const res = await fetch('/api/ppts?pageSize=12&sortBy=created_at&sortOrder=desc');
setFeaturedPPTs(items.slice(0, 8));
setNewPPTs(items.slice(0, 12));

// 修复后
const [featuredRes, newRes] = await Promise.all([
  fetch('/api/ppts?pageSize=8&sortBy=downloads&sortOrder=desc'),  // 编辑精选：下载最多
  fetch('/api/ppts?pageSize=12&sortBy=created_at&sortOrder=desc'), // 本周新品：最新
]);
```

**我的建议**: ✅ P0 完成后处理

---

### 问题 11: 首页 12 类 vs DB 9 类

| 维度 | 说明 |
|------|------|
| **现状** | 常量 12 类，DB 只有 9 类有数据 |
| **缺数据** | summary (0), report (0), plan (0) |
| **影响** | 用户点击后看到空页面 |
| **紧急度** | 🟠 中高 |
| **工作量** | 取决于方案 |

**方案对比**:

| 方案 | 做法 | 工作量 | 优点 | 缺点 |
|------|------|--------|------|------|
| A | 动态隐藏无数据分类 | 30分钟 | 用户体验好 | 需新增 API |
| B | 保持显示，空页面提示 | 5分钟 | 简单 | 用户可能失望 |
| C | 补充数据 | 运营工作 | 分类完整 | 需要内容 |
| D | 裁剪常量为 9 类 | 10分钟 | 代码一致 | 后续扩展需改代码 |

**我的建议**: 🤔 需要你决策
- 如果近期会补充数据 → 选 B
- 如果短期不会补充 → 选 A 或 D

---

### 问题 12: 分类图片缺失

| 维度 | 说明 |
|------|------|
| **现状** | 5 个分类用 placeholder.svg |
| **缺失** | technology, design, hr, medical, finance |
| **影响** | 视觉不完整 |
| **紧急度** | 🟠 中 |
| **工作量** | 30 分钟（准备图片） |

**图片规格**:
```
位置: /public/ppt/category-{slug}.jpg
尺寸: 400x240 (宽高比 5:3)
格式: JPG 或 PNG
大小: < 100KB
```

**需要的图片**:
1. `/ppt/category-technology.jpg` - 科技互联网
2. `/ppt/category-design.jpg` - 设计创意
3. `/ppt/category-hr.jpg` - 人力资源
4. `/ppt/category-medical.jpg` - 医疗健康
5. `/ppt/category-finance.jpg` - 金融财务

**我的建议**: 🤔 需要你决策 - 是设计师制作还是 AI 生成？

---

## 🟢 P2 - 体验优化 (后续)

### 问题 13: 分类数量硬编码

| 维度 | 说明 |
|------|------|
| **现状** | categoryMeta 中 count 是硬编码数字 |
| **应该** | 从 API 获取真实数量 |
| **影响** | 数据不准确 |
| **紧急度** | 🟢 低 |
| **工作量** | 20 分钟 |

**我的建议**: 后续优化

---

### 问题 14: 搜索仅简单 ilike

| 维度 | 说明 |
|------|------|
| **现状** | 简单 ilike 匹配 title/author/tags |
| **应该** | 分词、权重排序、相关性 |
| **影响** | 搜索结果不够精准 |
| **紧急度** | 🟢 低 |
| **工作量** | 较大 |

**我的建议**: 后续优化，可考虑 PostgreSQL 全文搜索

---

### 问题 15: 卡片可增加显示

| 维度 | 说明 |
|------|------|
| **可增加** | author, file_size |
| **影响** | 信息更丰富 |
| **紧急度** | 🟢 低 |
| **工作量** | 10 分钟 |

**我的建议**: 后续优化，根据用户反馈决定

---

## 📊 执行计划建议

### 第一批：P0 立即修复 (20分钟)

| 序号 | 问题 | 文件 | 预计 |
|------|------|------|------|
| 1 | DTO 映射 description | `src/actions/ppt/ppt.ts` | 2分钟 |
| 2 | DTO 映射 file_size | `src/actions/ppt/ppt.ts` | 2分钟 |
| 3 | 类型 file_size: number | `src/lib/types/ppt/ppt.ts` | 2分钟 |
| 4 | 过滤 deleted_at | `src/actions/ppt/ppt.ts` | 5分钟 |
| 5 | 下载按钮跳转 | `src/app/[locale]/(marketing)/ppt/page.tsx` | 5分钟 |

### 第二批：P1 文件清理 (10分钟)

| 序号 | 问题 | 操作 | 预计 |
|------|------|------|------|
| 6 | query-keys 引用 | 修改 import | 2分钟 |
| 7 | 删除 src/types/ppt.ts | rm | 1分钟 |
| 8 | 删除 src/schemas/ppt.ts | rm | 1分钟 |
| 9 | Zod 枚举 | 待决策 | - |

### 第三批：P1.5 业务优化 (待决策)

| 序号 | 问题 | 需要决策 |
|------|------|----------|
| 10 | 编辑精选/本周新品 | 是否分开查询？ |
| 11 | 12类 vs 9类 | 选择方案 A/B/C/D？ |
| 12 | 分类图片 | 图片来源？ |

---

## ❓ 待你决策的问题

| # | 问题 | 选项 | 我的建议 |
|---|------|------|----------|
| 1 | 是否立即执行 P0 修复？ | 是/否 | ✅ 是 |
| 2 | Zod schema 文件 | 同步/删除 | 删除 |
| 3 | 12类 vs 9类 | A/B/C/D | A (动态隐藏) |
| 4 | 分类图片来源 | 设计师/AI | AI 生成 |
| 5 | 编辑精选逻辑 | 按下载量/手动标记 | 按下载量 |

---

请逐一确认以上决策，我们就可以开始执行了。
