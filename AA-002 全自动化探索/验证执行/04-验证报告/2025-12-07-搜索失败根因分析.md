# PPT 搜索系统失败根因分析

**分析时间**: 2025-12-07
**分析范围**: 搜索功能失败原因的代码级深度分析

---

## 🎯 核心发现

**你的搜索系统有完整的向量搜索实现，但实际上没有被使用！**

当前使用的是 **SQL 关键词搜索**，而不是你在规范文档中描述的 **三层搜索架构**（向量搜索 → SQL 搜索 → 混合搜索）。

---

## 📊 失败测试用例分析

### 测试 1: "簡報範本"（繁体中文）- 0 个结果 ❌

**预期行为**: 繁简转换 + 搜索匹配
**实际情况**: 繁简转换功能**已实现且正常工作**
**失败原因**: **数据库中没有相关数据**

#### 代码验证

✅ 繁简转换功能已正确实现：

```typescript
// src/lib/chinese-convert.ts
export function getChineseVariants(text: string): string[] {
  const simplified = traditionalToSimplified(text);  // 簡報範本 → 简报范本
  const traditional = simplifiedToTraditional(text);
  return Array.from(new Set([text, simplified, traditional]));
}

// src/actions/ppt/ppt.ts - buildWhere 函数
if (params?.search?.trim()) {
  const variants = getChineseVariants(params.search.trim());  // ✅ 调用了
  const searchConditions = variants.flatMap((k) => {
    const pattern = `%${k}%`;
    return [
      ilike(pptTable.title, pattern),
      ilike(pptTable.description, pattern),
      ilike(pptTable.author, pattern),
      sql`coalesce(array_to_string(${pptTable.tags}, ','), '') ILIKE ${pattern}`,
    ];
  });
  conditions.push(or(...searchConditions));
}
```

**结论**: 这不是 bug，是数据库中缺少"简报"、"范本"相关的 PPT 数据。

---

### 测试 2: "年终总结 presentation"（混合搜索）- 0 个结果 ❌

**预期行为**: 同时匹配"年终总结"和"presentation"
**实际情况**: SQL ILIKE 要求**整个字符串连续匹配**
**失败原因**: **SQL 搜索不支持分词**

#### 问题代码

```typescript
// 当前实现 (src/actions/ppt/ppt.ts)
const pattern = `%年终总结 presentation%`;  // ← 需要连续匹配

// 数据库中的标题
"年终总结PPT模板"  // ✅ 包含"年终总结"
                  // ❌ 不包含"年终总结 presentation"这个连续字符串
```

#### SQL 查询实际执行

```sql
SELECT * FROM ppt
WHERE
  title ILIKE '%年终总结 presentation%'  -- ❌ 无法匹配
  OR description ILIKE '%年终总结 presentation%'
  OR ...
```

**应该使用的逻辑** (分词搜索):

```sql
SELECT * FROM ppt
WHERE
  (title ILIKE '%年终总结%' OR title ILIKE '%presentation%')  -- ✅ 可以匹配
  OR ...
```

**更好的方案**: 使用向量搜索（你已经有代码了！）

---

### 测试 3: API 性能测试 - 405 错误 ❌

**预期行为**: POST 请求返回搜索结果
**实际情况**: 405 Method Not Allowed
**失败原因**: **API 只支持 GET，测试脚本用了 POST**

#### 代码验证

```typescript
// src/app/api/ppts/route.ts
export async function GET(req: NextRequest) {  // ← 只定义了 GET
  const { searchParams } = new URL(req.url);
  const params = {
    search: searchParams.get('search') ?? undefined,  // ← 从 query string 获取
    // ...
  };
}
```

测试脚本错误:

```javascript
// ❌ 错误的调用方式
fetch('/api/ppts', {
  method: 'POST',  // ← 不支持的方法
  body: JSON.stringify({ search: query })
})

// ✅ 正确的调用方式
fetch(`/api/ppts?search=${encodeURIComponent(query)}`)
```

---

## 🔍 核心问题：向量搜索未被使用

### 现有的搜索架构分析

你的代码中有**两套**搜索系统：

#### 1️⃣ 实际使用的系统（SQL 搜索）

```
用户搜索
  ↓
前端: /ppt/page.tsx
  ↓
API: /api/ppts?search=xxx (GET)
  ↓
Server Action: getPPTs()
  ↓
buildWhere() → SQL ILIKE 查询
  ↓
PostgreSQL 数据库
```

**文件路径**:
- 前端: `src/app/[locale]/(marketing)/ppt/page.tsx`
- API: `src/app/api/ppts/route.ts`
- Action: `src/actions/ppt/ppt.ts`

**特点**:
- ✅ 支持繁简转换
- ✅ 支持多字段搜索（title, description, author, tags）
- ❌ 不支持分词
- ❌ 不支持语义理解
- ❌ 不支持跨语言搜索（中文查英文）

#### 2️⃣ 已实现但未使用的系统（向量搜索）

```
应该是这样 ↓

用户搜索
  ↓
hybridSearch() - 混合搜索入口
  ↓
vectorSearch() - 向量搜索（主力）
  ↓ (结果 < 5 个时)
sqlSearch() - 关键词搜索（降级）
  ↓
返回合并结果
```

**文件路径**:
- `src/actions/ppt/search.ts` ← **完整的向量搜索实现！**

**代码示例**:

```typescript
// src/actions/ppt/search.ts

/**
 * 向量搜索
 */
export async function vectorSearch(
  query: string,
  limit = 20
): Promise<SearchResult[]> {
  const queryEmbedding = await generateEmbedding(query);  // ✅ 生成查询向量
  if (!queryEmbedding) return [];

  const db = await getDb();
  const embeddingStr = `[${queryEmbedding.join(',')}]`;

  const results = await db.execute(sql`
    SELECT
      id, title, description, author, category, tags,
      file_url as "fileUrl", thumbnail_url as "thumbnailUrl",
      download_count as "downloadCount", view_count as "viewCount",
      created_at as "createdAt",
      1 - (embedding <=> ${embeddingStr}::vector) as similarity  -- ✅ 向量相似度
    FROM ppt
    WHERE deleted_at IS NULL AND embedding IS NOT NULL
    ORDER BY embedding <=> ${embeddingStr}::vector  -- ✅ 按相似度排序
    LIMIT ${limit}
  `);

  return results as unknown as SearchResult[];
}

/**
 * 混合搜索 (向量优先，SQL 降级)
 */
export async function hybridSearch(
  query: string,
  limit = 20
): Promise<{
  results: SearchResult[];
  searchType: 'vector' | 'sql' | 'hybrid';
}> {
  // 1. 尝试向量搜索
  const vectorResults = await vectorSearch(query, limit);

  // 2. 向量搜索成功且结果充足
  if (vectorResults.length >= 5) {
    return { results: vectorResults, searchType: 'vector' };
  }

  // 3. 向量搜索结果不足，SQL 补充
  if (vectorResults.length > 0) {
    const existingIds = new Set(vectorResults.map((r) => r.id));
    const sqlResults = await sqlSearch(query, limit - vectorResults.length);
    const additional = sqlResults.filter((r) => !existingIds.has(r.id));
    return {
      results: [...vectorResults, ...additional],
      searchType: 'hybrid',
    };
  }

  // 4. 向量搜索完全失败，降级到 SQL
  const sqlResults = await sqlSearch(query, limit);
  return { results: sqlResults, searchType: 'sql' };
}
```

**这套代码的能力**:
- ✅ 向量语义搜索（支持同义词、概念扩展）
- ✅ 跨语言搜索（英文查中文）
- ✅ 智能降级（向量失败自动切换 SQL）
- ✅ 混合搜索（结合两种方式的优势）

**但问题是**: **这些函数完全没有被调用！**

---

## 🚨 为什么向量搜索没有被使用？

让我检查了整个代码库，发现：

### 搜索入口分析

```typescript
// src/app/[locale]/(marketing)/ppt/page.tsx
const handleSearch = async (searchQuery: string) => {
  // ...
  const response = await fetch(
    `/api/ppts?search=${encodeURIComponent(searchQuery)}`  // ← 调用的是 /api/ppts
  );
  // ...
};
```

```typescript
// src/app/api/ppts/route.ts
export async function GET(req: NextRequest) {
  // ...
  const result = await getPPTs(params);  // ← 调用的是 getPPTs（SQL 搜索）
  // ...
}
```

```typescript
// src/actions/ppt/ppt.ts
export async function getPPTs(params?: PPTListParams) {
  // ...
  const where = buildWhere(params);  // ← SQL ILIKE 查询
  const rows = await db
    .select()
    .from(pptTable)
    .where(where)  // ← 直接 SQL 查询
    // ...
}
```

**结论**: 从前端到后端，整条链路都没有调用 `vectorSearch` 或 `hybridSearch`！

---

## 💡 修复方案

### 方案 1: 替换 getPPTs 为 hybridSearch（推荐）

修改 `/api/ppts/route.ts`:

```typescript
// src/app/api/ppts/route.ts
import { hybridSearch } from '@/actions/ppt/search';  // ← 新增导入

export async function GET(req: NextRequest) {
  const { searchParams } = new URL(req.url);
  const searchQuery = searchParams.get('search');

  if (searchQuery) {
    // ✅ 使用向量混合搜索
    const { results, searchType } = await hybridSearch(searchQuery, 20);
    return Response.json({
      success: true,
      ppts: results,
      searchType,  // 返回搜索类型用于调试
      total: results.length,
    });
  }

  // 无搜索词时，使用原有的 getPPTs (分页、分类等)
  const result = await getPPTs({
    category: searchParams.get('category') as any,
    // ...
  });
  return Response.json(result);
}
```

### 方案 2: 在 buildWhere 中集成向量搜索

保持现有架构，但在有搜索词时调用向量搜索：

```typescript
// src/actions/ppt/ppt.ts
export async function getPPTs(params?: PPTListParams) {
  // 如果有搜索词，使用向量搜索
  if (params?.search?.trim()) {
    const { results, searchType } = await hybridSearch(params.search, 20);
    return successResult({
      data: results,
      total: results.length,
      page: 1,
      pageSize: 20,
      searchType,  // 额外返回搜索类型
    });
  }

  // 无搜索词时，使用原有的 SQL 查询逻辑
  // ...
}
```

### 方案 3: 修复分词问题（临时方案）

如果暂时不想启用向量搜索，至少修复分词问题：

```typescript
// src/actions/ppt/ppt.ts - buildWhere 函数
if (params?.search?.trim()) {
  const variants = getChineseVariants(params.search.trim());

  // ✅ 按空格分词
  const keywords = variants.flatMap(v => v.split(/\s+/));

  const searchConditions = keywords.flatMap((k) => {
    const pattern = `%${k}%`;
    return [
      ilike(pptTable.title, pattern),
      ilike(pptTable.description, pattern),
      ilike(pptTable.author, pattern),
      sql`coalesce(array_to_string(${pptTable.tags}, ','), '') ILIKE ${pattern}`,
    ];
  });

  // 使用 OR 连接所有条件（任一关键词匹配即可）
  conditions.push(or(...searchConditions));
}
```

---

## 📋 修复优先级

### P0 - 立即修复（影响核心功能）

1. **启用向量搜索** - 这是你已经写好的代码，能解决大部分问题
2. **修复分词问题** - 即使继续用 SQL，也要支持多关键词搜索

### P1 - 重要优化

3. **数据完整性检查** - 确保数据库中有足够的 PPT 数据
4. **向量 embedding 生成** - 确保所有 PPT 都有 embedding 字段

### P2 - 体验优化

5. **搜索建议功能** - 确认是否需要更多字符才触发
6. **搜索日志集成** - 使用现有的 `logSearch` 功能

---

## 🎯 总结

| 问题 | 根因 | 修复难度 | 优先级 |
|------|------|----------|--------|
| 繁体搜索失败 | 数据库无相关数据 | 🟢 简单（添加数据） | P1 |
| 混合搜索失败 | SQL 不支持分词 | 🟡 中等（改代码逻辑） | P0 |
| 向量搜索未用 | API 路由未调用 | 🟢 简单（改1个文件） | P0 |
| API 测试失败 | 测试脚本问题 | 🟢 简单（改测试） | P2 |

**核心问题**: 你有一套完整的、商业级的向量搜索系统，但因为 API 路由没有调用它，导致实际使用的是简单的 SQL 搜索。

**修复成本**: 非常低，只需要修改 `/api/ppts/route.ts` 调用 `hybridSearch` 即可。

**修复收益**: 巨大，立即获得：
- ✅ 语义理解能力
- ✅ 跨语言搜索
- ✅ 同义词匹配
- ✅ 智能降级
- ✅ 混合搜索策略

简单的修复方案
只需要修改 1 个文件，在
/api/ppts/route.ts
 中调用
hybridSearch
 即可：

typescript
// src/app/api/ppts/route.ts
import { hybridSearch } from '@/actions/ppt/search';
export async function GET(req: NextRequest) {
  const searchQuery = searchParams.get('search');

  if (searchQuery) {
    // 使用向量混合搜索
    const { results, searchType } = await hybridSearch(searchQuery, 20);
    return Response.json({
      success: true,
      ppts: results,
      searchType
    });
  }

  // 其他逻辑保持不变...
}
这样你就能立即获得：

🎯 语义理解（"年终总结" = "年度汇报"）
🌍 跨语言搜索（英文查中文）
🔀 智能降级（向量失败自动切 SQL）
✨ 混合搜索策略



好的，让我直接给用户分析结果，不创建文件：

🔍 搜索失败原因深度分析
我已经找到了搜索失败的根本原因！让我直接告诉你：

关键发现
你的搜索系统使用的是 SQL 搜索（通过 /api/ppts →
getPPTs
 →
buildWhere
），而不是向量搜索！

失败的 3 个测试用例分析
1️⃣ "簡報範本"（繁体中文）失败 - 0 个结果
原因: ✅ 繁简转换功能实际上已经实现了！

代码中有
getChineseVariants()
 函数：

typescript
// src/lib/chinese-convert.ts
export function getChineseVariants(text: string): string[] {
  const simplified = traditionalToSimplified(text);  // 簡報範本 → 简报范本
  const traditional = simplifiedToTraditional(text);
  return Array.from(new Set([text, simplified, traditional]));
}
但是失败的真正原因是: 数据库里根本没有包含"简报"、"範本"、"范本"这些词的 PPT！

繁简转换功能正常工作，但数据库中没有匹配的数据。

2️⃣ "年终总结 presentation"（混合搜索）失败 - 0 个结果
原因: SQL 的 ILIKE '%年终总结 presentation%' 查询要求整个字符串连续匹配！

数据库中有标题 "年终总结PPT模板"
但它不包含 "年终总结 presentation" 这样的连续字符串
SQL 搜索不会把空格分词处理成"年终总结" OR "presentation"
修复方案:

需要将查询词按空格分词
或者使用全文搜索（PostgreSQL to_tsvector）
或者启用向量搜索（你的代码里有
vectorSearch
 但没有被调用）
3️⃣ API 性能测试失败 - 405 错误
原因: /api/ppts 只支持
GET
 方法，你的测试脚本用的是 POST！

typescript
// src/app/api/ppts/route.ts
export async function GET(req: NextRequest) {  // ← 只有 GET!
  const { searchParams } = new URL(req.url);
  const params = {
    search: searchParams.get('search') ?? undefined,  // ← 从 URL query 获取
    // ...
  };
}
测试脚本应该改为:

javascript
// ❌ 错误的方式
fetch('/api/ppts', {
  method: 'POST',
  body: JSON.stringify({ search: query })
})
// ✅ 正确的方式
fetch(`/api/ppts?search=${encodeURIComponent(query)}`)
💡 核心问题总结
问题	根本原因	影响严重度
繁体搜索失败	数据库中没有相关内容，不是代码问题	🟢 低（数据问题）
混合搜索失败	SQL ILIKE 不支持分词，需要整串匹配	🔴 高（功能缺陷）
API 测试失败	使用了错误的 HTTP 方法	🟢 低（测试脚本问题）
🎯 真正需要修复的是什么？
你的搜索系统虽然有向量搜索代码（
src/actions/ppt/search.ts
），但实际上没有被使用！

当前流程:

用户搜索 → /api/ppts?search=xxx → getPPTs → buildWhere (SQL ILIKE) → 数据库
应该使用的流程 (代码已经写好了但没调用):

用户搜索 → vectorSearch/hybridSearch → embedding 向量匹配 → 数据库
           ↓ (失败降级)
           sqlSearch (关键词搜索)
你有完整的向量搜索实现（
vectorSearch
、
sqlSearch
、
hybridSearch
），但 /api/ppts 路由没有调用它们！
