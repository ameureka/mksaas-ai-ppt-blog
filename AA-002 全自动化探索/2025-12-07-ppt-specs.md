## 🎯 最终执行方案：清理旧数据并重建向量

### 📂 文件组织结构
所有相关文件将保存到：`/Users/ameureka/Desktop/mksaas-ai-ppt-blog/AA-002 全自动化探索/验证执行/`

```
验证执行/
├── 01-数据清理/
│   ├── clean-old-embeddings.sql    # 清理旧向量的SQL
│   └── backup-stats.txt             # 备份统计信息
├── 02-代码更新/
│   ├── embedding-config.md          # 配置说明
│   └── test-results.txt             # 测试结果
├── 03-执行日志/
│   ├── generation.log                # 向量生成日志
│   └── verification.log              # 验证日志
└── 04-验证报告/
    └── final-report.md               # 最终验证报告
```

### 🔄 执行步骤（清理重建方案）

#### 步骤 1：备份当前状态
```sql
-- 查看当前向量状态（记录到 backup-stats.txt）
SELECT 
  COUNT(*) as total_ppts,
  COUNT(embedding) as with_embedding,
  COUNT(DISTINCT embedding_model) as model_types,
  STRING_AGG(DISTINCT embedding_model, ', ') as models_used
FROM ppt;
```

#### 步骤 2：清理旧向量数据
```sql
-- 文件：01-数据清理/clean-old-embeddings.sql
-- 清空所有现有向量，准备重新生成
UPDATE ppt 
SET 
  embedding = NULL, 
  embedding_model = NULL,
  updated_at = NOW()
WHERE embedding IS NOT NULL;

-- 验证清理结果
SELECT COUNT(*) as remaining_embeddings FROM ppt WHERE embedding IS NOT NULL;
-- 应该返回 0
```

#### 步骤 3：更新代码配置
需要修改 2 个文件：

**文件 1：`src/lib/embedding.ts`**
```typescript
// 第8行 - 确认模型配置
const EMBEDDING_MODEL = 'BAAI/bge-m3';  // ✅ 使用最佳模型
```

**文件 2：`scripts/generate-embeddings.ts`**
```typescript
// 第35行 - 更新模型记录
embedding_model = 'BAAI/bge-m3',  // 从 'BAAI/bge-large-zh-v1.5' 改为 'BAAI/bge-m3'
```

#### 步骤 4：执行向量生成
```bash
# 确保环境变量设置
export SILICONFLOW_API_KEY="sk-slbdjnzyadnecpystbwtnzhgwexekhqcygifgtrslqakntux"

# 运行生成脚本（预计 2-5 分钟）
pnpm generate-embeddings | tee "AA-002 全自动化探索/验证执行/03-执行日志/generation.log"

# 更新热门关键词
pnpm update-hot-keywords
```

#### 步骤 5：验证搜索功能
```bash
# 1. 重启开发服务器
pkill -f "next dev"
pnpm dev &

# 2. 运行测试脚本
pnpm exec tsx scripts/test-search-api.ts | tee "AA-002 全自动化探索/验证执行/03-执行日志/verification.log"

# 3. 手动测试
# 访问 http://localhost:3005/ppt
# 测试查询：
# - "商业计划书" （中文）
# - "Business Plan" （英文）
# - "PPT模板" （中英混合）
# - "年终总结 presentation" （混合句子）
```

#### 步骤 6：生成最终报告
创建 `04-验证报告/final-report.md`，包含：
- 生成的向量总数
- 成功/失败统计
- 搜索响应时间
- 多语言搜索效果

### ⏱️ 执行时间估算

| 步骤 | 预计时间 | 说明 |
|------|----------|------|
| 备份状态 | 10秒 | SQL 查询 |
| 清理数据 | 10秒 | UPDATE 语句 |
| 更新代码 | 30秒 | 2个文件 |
| 生成向量 | 3-5分钟 | 取决于PPT数量，每个200ms延迟 |
| 验证测试 | 2分钟 | API测试+手动测试 |
| **总计** | **6-8分钟** | |

### ✅ 成功标准

1. **数据清理完成**
   - `SELECT COUNT(*) FROM ppt WHERE embedding IS NOT NULL` 返回 0

2. **向量生成成功**
   - 所有 PPT 都有 embedding
   - embedding_model 字段都是 'BAAI/bge-m3'

3. **搜索功能正常**
   - 中文查询返回相关结果
   - 英文查询返回相关结果
   - 中英混合查询正常工作
   - 响应时间 < 500ms

### ⚠️ 注意事项

1. **数据安全**：清理前会记录统计信息
2. **中断恢复**：如果生成中断，重新运行会自动跳过已完成的
3. **API 限流**：已有 200ms 延迟保护
4. **日志记录**：所有操作都会保存日志到指定目录

### 📊 预期结果

执行完成后，您将获得：
- 所有 PPT 使用统一的 BAAI/bge-m3 模型生成的向量
- 支持中英文混合搜索
- 更好的搜索准确度（bge-m3 > bge-large-zh-v1.5）
- 完整的执行日志和验证报告

**确认执行此方案？**